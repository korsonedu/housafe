# Task 6 Report: 时序事件存储与查询（events）

## Status: DONE

## Files Changed

### Created
- `backend/events/__init__.py`
- `backend/events/apps.py`
- `backend/events/models.py` — 4 concrete event tables: PostureRow, PresenceRow, VitalRow, OccupancyRow
- `backend/events/store.py` — idempotent `store_event()` with `IntegrityError` handling via savepoint
- `backend/events/views.py` — `TodayView` for per-room latest-state query
- `backend/events/urls.py`
- `backend/events/serializers.py` (placeholder)
- `backend/events/migrations/0001_initial.py` — auto-generated by makemigrations
- `backend/events/migrations/0002_hypertables.py` — Timescale hypertable migration (guarded)
- `backend/events/tests/__init__.py`
- `backend/events/tests/test_store.py` — idempotent write test
- `backend/events/tests/test_query.py` — today endpoint test

### Modified
- `backend/housafe/settings.py` — added `"events"` to INSTALLED_APPS
- `backend/housafe/urls.py` — added `path("api/", include("events.urls"))`

## TDD Evidence

### test_store_and_idempotent
1. **RED**: Wrote test → ran against SQLite → `IntegrityError` broke the Django test transaction (savepoints missing in initial `store_event`)
2. **GREEN**: Wrapped `Row.objects.create()` in `transaction.atomic()` so duplicate `(device_id, seq)` triggers a savepoint-level `IntegrityError` without corrupting the outer transaction
3. Verified: first insert succeeds, second insert silently ignored, count=1 and ts matches source event

### test_today_returns_latest_per_room
1. **RED**: Wrote test → 404 (no URL/view)
2. **GREEN**: Created `TodayView` + `events/urls.py` + wired into project urls
3. Verified: stores 2 posture events for same device, GET returns latest posture="walk"

### Full suite
All 8 tests pass across accounts, families, devices, and events apps.

## Hypertable Migration

- Created `0002_hypertables.py` using `RunPython` (not `RunSQL`)
- Guard: `if schema_editor.connection.vendor != 'postgresql': return` — silently skips on SQLite
- On PostgreSQL with TimescaleDB, executes `create_hypertable()` for all 4 event tables
- Verified: `python manage.py migrate` on SQLite applies 0001 + 0002 without errors

## Module Coupling Check

| Module | Imports | Cross-app? | Status |
|--------|---------|------------|--------|
| `events/models.py` | `django.db.models` | No | OK |
| `events/store.py` | `django.db` + `events.models` | No | OK |
| `events/views.py` | `families.models.Family` + `events.models` | Yes (models layer only) | OK per brief |

- `device_id` is a plain `CharField(40)` — no FK to devices, no import from `devices.models`
- No import from `families.views` internals
- The `Family.objects.get()` in TodayView is explicitly provided in the brief's view code for ownership authorization

## Self-Review

- `store_event` uses `transaction.atomic()` savepoints, which is the correct Django pattern for idempotent writes when tests use transactions
- `ts` field always sourced from the event model's own `ts` — never from `time.time()` or server clock
- `ts_recv` passed explicitly by the caller
- Heartbeat is NOT stored — `_MAP` only has 4 keys: posture, presence, vital, occupancy
- Unique constraints are on `(device_id, seq)` only (not including `ts`) — this is the author-asserted idempotency key from the contract models
