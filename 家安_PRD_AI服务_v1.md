# 家安 housafe · AI 分析服务 PRD

> 版本 v3.0 | 2026-07-12 | 架构变更：统一隐空间世界模型（ELPD）底座
> v2.0→v3.0 主要变更：AI 服务不再是一系列独立算法模块（ALG-0~6）组成的管线，而是一个**统一隐空间世界模型**——Encoder 将多模态输入编码为隐空间表征，Predictor 学习"人+屋子"的动力学，各任务通过轻量 Decoder 从同一隐空间读取。原 ALG-0~6 的能力映射为世界模型的组件，不再是独立运行的模块。
> v1.0→v2.0 主要变更：AI 服务不再做"厂商固件的二次判断"，而是从点云流直接做感知。
> 子系统 PRD ③ / ④ · 负责角色：AI / ML 工程师
> 范围：构建统一隐空间世界模型——Encoder、Latent Space、Predictor、任务 Decoders、干预接口。从后端接收点云流，输出融合事件、异常判断、通知等级、个人基线、报告统计。
> 架构原理与算法映射见《家安_PRD_总纲_v3.md》§3 核心架构；模型内部实现细节见《家安_核心算法设计文档_v3.md》。
> 上下游：从 ② 后端取点云流 / 时序数据，结果回 ②。

---

## 1. 模块边界

**AI 服务负责**：统一隐空间世界模型的全部组件——

| 组件 | 职责 | 一句话 |
|------|------|--------|
| **Encoder** | 多模态输入（雷达点云 + 时间编码 + 未来:门磁/温湿度）→ 统一隐空间表征 S_t | "把世界压缩成一个向量" |
| **Latent Space** | 图结构（空间拓扑、区域类型）+ 向量（人状态、观测不确定性、全局上下文）。唯一真相源 | "屋子现在是什么状态" |
| **Predictor** | S_t + 可选干预 I_t → S_{t+1}。自监督学习"人+屋子"的演化动力学。多尺度预测 | "接下来会发生什么" |
| **Decoder: 异常** | 从 S_t 读取当前状态在该上下文中偏离预期的距离 → 异常分。空间感知 | "这正常吗" |
| **Decoder: 通知** | 异常分 + 持续性 + 紧急性先验 → 通知等级 | "要不要告诉子女" |
| **Decoder: 报告** | 过去 N 天的隐状态轨迹摘要 → 结构化数据/自然语言 | "这周怎么样" |
| **Decoder: 追踪** | 位置编码在图上随时间的变化 → 轨迹 | "人在哪儿" |
| **干预接口** | 干预向量注入 Predictor。反事实推理 | "如果这样做会怎样" |

**AI 服务不负责**：数据持久化存储、通知多通道下发（②）；点云采集（①）；界面（④）。

---

## 2. 功能需求（按世界模型组件组织）

### 2.1 Encoder（编码器）

| 编号 | 需求 | 说明 | 分期 |
|------|------|------|------|
| FR-A0 | 点云→隐空间编码 | 输入单雷达点云帧流，输出隐状态向量。姿态/存在/人数/生命体征不再是独立输出标签，而是隐空间中的子空间。Encoder 是 PointNet-like 架构 + 时序聚合（sliding window），不输出离散分类结果 | MVP |
| FR-A1 | 多雷达 cross-attention 融合 | 多个雷达的点云流通过 cross-attention 融合到同一隐空间。雷达间的空间关系不是规则定义的，是学出来的。取代原 ALG-1 的"软时钟对齐 + 规则拼接" | MVP |
| FR-A2 | 时间编码 | 时辰（循环编码）、星期、季节注入隐空间的全局上下文向量 | MVP |
| FR-A3 | 观测不确定性编码 | Encoder 输出每个维度的 uncertainty，决定 Predictor 在预测时更信模型还是更信观测 | MVP |
| FR-A4 | 多模态扩展接口 | 预留门磁、温湿度、光照等额外模态的编码入口。新模态 = 新 Encoder 分支，输出到同一隐空间 | P2 |

### 2.2 Latent Space（隐空间）

| 编号 | 需求 | 说明 | 分期 |
|------|------|------|------|
| FR-B1 | 图结构——空间拓扑 | 节点：学出来的空间区域（不需要人工标注"这是床"，但需要能区分不同功能区域）。边：空间关系（邻接、包含、可达）。慢变量，数天到数周更新一次 | MVP |
| FR-B2 | 图结构——区域属性向量 | 每个图节点的属性：区域类型倾向（学出来的，如"可躺卧表面""过渡通道""停留区域"）、危险等级 | MVP |
| FR-B3 | 向量——人状态 | 位置编码（映射到图上的位置）、姿态编码、运动方向+速度、生命体征编码、活动意图 | MVP |
| FR-B4 | 向量——全局上下文 | 时间编码、家中人数、近期事件摘要、系统状态 | MVP |
| FR-B5 | 个人基线（流形分布） | 14 天隐状态轨迹 → 该老人"正常"的流形分布。基线不是独立模块，是隐空间上的概率密度估计 | MVP |
| FR-B6 | 多人家属扩展 | 隐空间支持多个独立的人状态向量。P2 实现 | P2 |

### 2.3 Predictor（预测器）

| 编号 | 需求 | 说明 | 分期 |
|------|------|------|------|
| FR-C1 | 自监督动力学学习 | 训练目标：给定前 N 帧隐状态序列，预测下一帧隐状态。损失 = 预测 S_{t+1} vs 真实 Encoder(点云_{t+1}) 的距离。所有正常日子的数据都是训练信号，无需人工标注 | MVP |
| FR-C2 | 多尺度预测 | 1s / 10s / 60s / 3600s 预测头。短尺度用于实时异常检测，长尺度用于模式偏离 | MVP |
| FR-C3 | 物理约束学习 | 从数据中自然学到：人不能穿墙、运动轨迹连续、某些区域之间不可直达 | MVP |
| FR-C4 | 预测偏差 = 异常信号 | Predictor 输出的预测 S_{t+1} 与真实 S_{t+1} 的偏差，本身就是最重要的异常信号——"系统没想到会这样"。这是取代原 ALG-2（状态机跌倒确认）的机制 | MVP |
| FR-C5 | 干预注入 | Predictor 接受可选的干预向量 I_t："通知子女"→可能触发"子女打电话"→"老人接电话"→行为改变。用于反事实推理 | P2 |

### 2.4 Decoders（解码器——轻量，不做重推理）

| 编号 | 需求 | 说明 | 分期 |
|------|------|------|------|
| FR-D1 | 异常解码器 | 从 S_t 读取：当前隐状态在该上下文（时间+区域）中离预期分布的距离 → 异常分。空间感知："躺着"在浴室地上→高分，在床上→低分。多维度偏离自动耦合（活动+睡眠+心率），不需要外部融合算法 | MVP |
| FR-D2 | 通知解码器 | 异常分 + 持续性（持续多少帧偏离）+ 紧急性先验 → 通知等级（不提/推送/短信/电话） | MVP |
| FR-D3 | 报告解码器 | 过去 N 天的隐状态轨迹 → 活动摘要、睡眠统计、异常清单、趋势对比。可以接 LLM 做自然语言生成（P2） | MVP |
| FR-D4 | 追踪解码器 | 位置编码在图上的时序轨迹 → 热力图、活动路径、房间停留时长 | MVP |
| FR-D5 | 生命体征解码器 | 从隐空间的安静态子空间读取呼吸率/心率序列 | MVP |

---

## 3. 冷启动策略

世界模型的三个阶段训练（详见总纲 §3.4），对应产品的冷启动行为：

| 阶段 | 时间 | 数据 | 产品表现 |
|------|------|------|---------|
| **部署即有** | Day 0 | 合成数据预训练 | Encoder 具备基础人形感知能力。Predictor 有基础物理常识（人不能穿墙、运动连续）。跌倒检测即时可用 |
| **空间学习** | Day 1-7 | 该家庭点云流 | 隐空间的图结构收敛——学出房间拓扑、功能区域。跨房间追踪准确率逐日提升 |
| **个人化** | Day 8-15 | 该老人日常数据 | 个人基线（隐状态流形分布）建立。第 15 天起模式偏离检测上线 |
| **持续进化** | Day 15+ | 持续流入 | Predictor 持续自监督学习。基线在线更新。模型随使用越来越好 |

**Fallback 规则层：** 在世界模型置信度低时（如极端环境、新雷达型号），内置轻量规则引擎接管。这不是架构退步，是安全网。

---

## 4. 接口契约

- **入（从 ② 后端取）**：实时点云流、时序对齐后的多雷达数据、历史事件查询、时间戳
- **出（回 ② 后端）**：
  - 实时：隐状态摘要（供后端记录，不传输完整隐状态向量）、异常告警、通知等级
  - 定期：个人基线模型更新、周报统计数据
  - 按需：隐状态轨迹（供报告生成）、干预推理结果

---

## 5. 指标（验收）

| 指标 | 目标 | 说明 |
|------|------|------|
| 跌倒检测准确率 | ≥ 90% | 端到端：点云→通知 |
| 误报 | < 2 次/周/户 | |
| Predictor 预测误差 | 优于 naive baseline（"人不动"）50%+ | 自监督学习的核心指标 |
| 隐空间一致性 | 同一老人在同一区域同一姿态，不同时间的隐状态余弦相似度 > 0.85 | 表征质量指标 |
| 跨房间追踪准确率 | ≥ 85%（单人场景） | |
| 编码延迟 | 点云到达 → S_t 输出 < 100ms | 实时性要求 |
| 预测延迟 | S_t → 预测 S_{t+1} < 50ms | |

---

## 6. 训练基础设施

| 需求 | 说明 | 分期 |
|------|------|------|
| 合成数据生成管线 | 雷达射线追踪模拟器 + 人体骨骼模型 + 室内户型生成器。用于阶段 0 预训练 | MVP |
| 自监督训练管线 | 在线/离线训练。每户每天 ~1-2GB 点云，50 户 50-100GB/天。需 GPU 集群（单卡 A100 可处理 20-30 户的在线学习） | MVP |
| 模型版本管理 | 每个家庭的隐空间 + Predictor 是独立的（至少图部分是独立的）。需要管理 N 个模型实例的版本和更新 | MVP |
| 评估框架 | 下游任务端到端评估 + 隐空间内部质量指标 + Predictor 预测误差 | MVP |

---

## 7. 数据与演进

- 依赖 ② 的点云流
- 阶段 0：合成数据预训练 Encoder + Predictor（雷达模拟器 + 人体模型 + 户型生成）
- 阶段 1：内测用户真实点云流 → 自监督学习。每个家庭维护独立的世界模型实例（图结构不同，但 Encoder/Predictor 底座共享）
- 阶段 2：大规模用户 → 预训练世界模型底座 → 新用户 fine-tune 自家空间拓扑 → 数据飞轮闭环
- 演进主线：合成数据预训练 → 帧级自监督 → 时序自监督 → 跨家庭泛化

---

## 8. 开放项

| 编号 | 事项 |
|------|------|
| OQ-A1 | 合成数据预训练的 Encoder 迁移到真实点云的 domain gap。需实测确定需要的 fine-tune 数据量 |
| OQ-A2 | 隐空间维度（候选 256/512）和图节点粒度（房间级 vs 区域级）的超参 |
| OQ-A3 | 自监督目标函数选型：对比学习（SimCLR-like）vs 掩码预测（MAE-like）vs JEPA-like 直接预测 |
| OQ-A4 | 多家庭模型底座共享 vs 每家庭独立模型。倾向：Encoder/Predictor 底座共享，图结构+基线独立 |
| OQ-A5 | 点云上云隐私合规——世界模型架构下点云仅用于实时 Encoder 推理，原始点云不持久化。合规影响需 PIA 评估 |
| OQ-A6 | Predictor 的多尺度训练策略——联合训练 vs 分尺度训练 |
| OQ-A7 | 干预推理的评估方法——没有 ground truth，如何验证反事实推理的正确性 |
| OQ-A8 | Fallback 规则层与世界模型的切换逻辑——什么条件下退回到规则 |
