# 家安 housafe · 云平台/后端 PRD

> 版本 v3.0 | 2026-07-12 | 架构变更：统一隐空间世界模型（ELPD）底座
> v2.0→v3.0 主要变更：AI 子系统已升级为统一隐空间世界模型（ELPD: Encoder → Latent Space → Predictor → Decoders）。后端不再向"ALG 管线"分发数据，改为向世界模型提供点云流和时序数据。点云仅用于实时推理，原始点云不持久化（合规简化）。
> v1.0→v2.0 主要变更：接收点云流而非结构化事件；点云存储/分发新增；商业模式从订阅制改为买断制。
> 子系统 PRD ② / ④ · 负责角色：后端工程师
> 范围：从接收端侧点云流，到存储、分发世界模型、通知下发、报告生成、账户与设备管理、合规执行的全部云端服务。AI 世界模型本身在 ③。
> 边界外：世界模型实现见《家安_核心算法设计文档_v3.md》；端侧数据契约见《家安_PRD_端侧_v1.md》；App 交互见《家安_PRD_App_v1.md》；全局定位/分期见《家安_PRD_总纲_v3.md》。
> 上下游：上游 ① 端侧（点云流 + 心跳）；调用 ③ AI 服务（世界模型）；对接 ④ App。

---

## 1. 模块边界

**后端负责**：设备接入网关、时序存储、数据分发给 AI、通知多通道下发与升级、报告生成、账户 / 家庭 / 设备管理、隐私合规执行（存储 / 加密 / 导出删除 / 授权状态）。

**后端不负责**：多雷达融合与异常判断（在 ③ AI 服务）、端侧采集（①）、界面（④）。

---

## 2. 功能需求

### 2.1 设备接入与数据管道

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-B1 | 设备接入网关：接收端侧上传（点云流 + 生命体征 + 心跳。协议候选 MQTT over TLS / WebSocket），鉴权、限流、写入 | MVP |
| FR-B2 | 点云与事件存储：点云帧入对象存储（短期缓冲），结构化事件入时序库（TimescaleDB 候选），活动数据保留 90 天 | MVP |
| FR-B3 | 数据分发：向 ③ 提供实时点云流 + 历史事件查询接口 | MVP |
| FR-B4 | 设备注册与生命周期：配网注册、在线状态、心跳监控、离线判定 | MVP |

### 2.2 通知下发

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-B5 | 多通道通知网关：App 推送（APNs）、短信、电话语音，三方对接 | MVP |
| FR-B6 | 分级执行：接收 ③（ALG-6）的通知等级决策，路由到对应通道 | MVP |
| FR-B7 | 通知升级：无响应按策略升级（推送 → 短信 → 电话）+ 联系人顺序 | MVP |
| FR-B8 | 设备离线 / 断电主动提醒（对应 ① FR-E10） | MVP |

### 2.3 报告

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-B9 | 周报生成：模板 + 统计（活动 / 睡眠 / 生命体征 / 异常清单） | MVP |
| FR-B10 | 月报 PDF 导出 | P2 |
| FR-B11 | 报告 LLM 生成（人工审核规则） | P2 |

### 2.4 账户与设备管理

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-B12 | 账户体系：子女账户注册 / 登录 / 鉴权 | MVP |
| FR-B13 | 家庭与成员：多联系人、紧急联系人、通知顺序 | MVP |
| FR-B14 | 设备管理接口：绑定 / 解绑 / 重命名 / 指示灯开关 / OTA 触发（供 ④ 调用） | MVP |

### 2.5 隐私合规执行（横切）

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-B15 | 数据分级与保留期：点云帧短期缓冲后即弃（仅用于实时推理）；活动事件 90 天；基线随用户 | MVP 前 |
| FR-B16 | 数据导出 / 删除接口 | MVP |
| FR-B17 | 生命体征单独授权状态管理 | MVP |
| FR-B18 | 加密：传输 TLS、存储 AES-256 | MVP |
| FR-B19 | 数据隔离：个人基线归属用户、不跨用户训练 | MVP |
| FR-B20 | 点云隐私评估：点云上云是否可逆向重建室内活动（PIA），评估通过后方可上线 | MVP 前 |
| FR-B21 | 数据泄露应急响应预案 | P2 |

---

## 3. 接口契约

- **入**：端侧点云流 + 生命体征 + 心跳（见 ① §3）
- **内**：向 ③ 提供点云流（实时 + 短期历史）+ 事件查询；接收 ③ 的融合事件 / 告警 / 通知等级 / 基线模型
- **出**：向 ④ 提供 REST / WebSocket API（今日状态、告警列表、报告、设备、账户、家庭成员、数据权利）

---

## 4. 非功能需求

| 编号 | 需求 | 目标 |
|------|------|------|
| NFR-B1 | 紧急通知端到端时延 | 数秒级 |
| NFR-B2 | 服务可用性 | 目标待定（关乎安全告警，不能长时间中断） |
| NFR-B3 | 存储与并发 | 随设备规模水平扩展 |
| NFR-B4 | 数据安全 | 传输 / 存储加密 + 访问审计 |

---

## 5. 依赖与开放项

| 编号 | 事项 |
|------|------|
| OQ-B1 | 通知三方选型（短信 / 语音呼叫服务商）与合规 |
| OQ-B2 | 时序库选型（TimescaleDB 等） |
| OQ-B3 | 接入协议与 ① 端侧对齐 |
| OQ-B4 | 电话语音呼叫的合规与触达方式 |
