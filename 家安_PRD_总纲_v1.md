# 家安 housafe · PRD 总纲

> 版本 v3.0 | 2026-07-12 | 架构变更：统一隐空间世界模型（ELPD）底座
> v2.0→v3.0 主要变更：AI 子系统从"分模块算法管线（ALG-0~6 独立运行）"改为"统一隐空间世界模型（ELPD: Encoder → Latent Space → Predictor → Decoders）"。核心洞察：家庭是世界上最标准的受限世界——2-4 个房间、1-2 个人、稳定重复的日常模式。复杂度刚好够形成技术壁垒，又不至于不可解。统一隐空间让所有信号在同一个几何空间里彼此约束、互相解释，从根本上消除了管道架构中"信息被逐层硬决策丢弃"的问题。
> v1.0→v2.0 主要变更：硬件选型从森思泰克改为 TI IWR6843；架构约束从"点云不上云"改为"点云上云做自研感知"；商业模式从订阅制改为买断制。
> 定位：产品全局定位、目标用户、功能边界、分期、成功指标、核心算法总览。各子系统的功能细节见 4 份子 PRD。
> 商业内容（定价 / SKU / 竞品 / 利润模型）见《家安_战略论证_v4.md》，不在 PRD 体系内。

---

## 0. 文档集与架构基线

| 文档 | 职责 |
|------|------|
| **家安_PRD_总纲_v1.md**（本文） | 全局定位 / 边界 / 分期 / 指标 / 算法总览 / 子 PRD 索引 |
| 家安_PRD_端侧_v1.md ① | 雷达设备端固件与软件 · 嵌入式（v2.0：输出点云流，不集成厂商固件） |
| 家安_PRD_后端_v1.md ② | 云平台 / 后端 · 后端 |
| 家安_PRD_AI服务_v1.md ③ | AI 分析服务（承载核心算法）· AI/ML |
| 家安_PRD_App_v1.md ④ | 子女 App（iOS）· iOS |
| 家安_核心算法设计文档_v2.md | 算法技术方案（v2.0：自研点云感知） |
| 家安_战略论证_v3.md | 为什么做 / 能不能做 / 怎么赚钱 |

**架构基线决策（贯穿所有子系统，锁定）：**
- 无本地网关，每雷达 WiFi 直连云
- **统一隐空间世界模型底座**：雷达端侧输出原始点云流 → 云端 Encoder 压缩为统一隐空间表征 → Predictor 学习"人+屋子"的动力学演化 → 所有下游任务（跌倒/静止/偏离/通知/报告）从同一隐空间通过轻量 Decoder 读取。这是从"分模块算法管线"到"统一世界模型"的架构范式转换
- **自研点云感知**：雷达端侧输出原始点云流 → 云端 GPU 做点云→姿态→跌倒→生命体征。在 v3.0 中，这是 Encoder 的核心能力——点云编码到隐空间，而非输出离散事件标签
- 纯雷达路线（不接手环）
- 无摄像头 / 无视频（硬边界）
- 无语音交互（MVP）
- MVP 2 雷达起步（浴室 + 卧室）
- 硬件买断制，云服务基础功能免费，高级报告可选付费

---

## 1. 产品概述

### 1.1 定位

家安 housafe 是一套面向居家养老的非接触式 AI 健康监测系统。用毫米波雷达组网无感采集老人在家中的活动与生命体征，AI 做异常检测与趋势分析，异地子女通过 App 掌握父母状态。

一句话：**让异地子女 30 秒看懂父母今天在家过得好不好，异常时第一时间知道。**

### 1.2 目标用户

| 角色 | 画像 | 与产品的关系 |
|------|------|------------|
| 付费者 / 主用户 | 35–50 岁，与 65 岁以上父母异地居住，有稳定收入 | 购买、安装、日常使用 App、接收通知 |
| 被监测者 | 65 岁以上，居家养老，独居或与配偶同住 | 不操作任何设备或软件，无感被监测 |

### 1.3 核心价值

无感（不穿戴、不操作、无摄像头）、准（多雷达交叉 + 个人基线降误报）、有预判（趋势偏离发现就医信号）、不骚扰（分级通知）。

### 1.4 功能边界

**做**：活动数据采集、安静态生命体征采集、四类异常检测（跌倒 / 长时间静止 / 生命体征异常 / 日常模式偏离）、分级通知、趋势报告。

**不做（硬边界）**：摄像头 / 视频、紧急救援调度（120）、医疗诊断、药物管理、语音交互、雷达能力外指标（血压 / 血氧 / 血糖）。

### 1.5 核心场景

- S1 跌倒告警：浴室滑倒 → 姿态突变 + 交叉验证 → 确认 → App 强推 + 短信 + 电话
- S2 误报抑制：蹲下捡物 → 二次确认恢复行走 → 不通知，仅记录
- S3 静止超时：卧室长时间无活动且超基线 → 分级提醒
- S4 模式偏离：连续 3 天活动量 < 基线 50% + 起床推迟 → 多信号叠加 → 建议关注
- S5 日常安心：首页显示"状态正常，昨夜睡眠 7.2h，起床 6:40"
- S6 就医参考：导出近 30 天健康趋势 PDF 供医生参考

---

## 2. 分期总览

| 分期 | 时间 | 硬件形态 | 目标 |
|------|------|---------|------|
| MVP | 0–6 个月 | 2 雷达起步（浴室 + 卧室），TI IWR6843，WiFi 直连云 | 验证自研点云感知管线、延迟确认降误报、个人基线、分级通知 |
| Phase 2 | 6–18 个月 | 4 雷达全屋（S/L 套） | 全屋覆盖、生命体征异常、步态预测、Android、月报 PDF |
| Phase 3 | 18 个月+ | 国产 60GHz SoC | 规模化降本 |

**子系统分期矩阵：**

| 子系统 | MVP | Phase 2 |
|--------|-----|---------|
| ① 端侧 | 2 雷达点云采集 / 基础去噪 / 点云上传 / 配网 / OTA | 端侧轻量推理 fallback、超级电容断电消息 |
| ② 后端 | 接入 / 点云缓冲与分发 / 时序存储 / 通知下发 / 报告 / 账户设备管理 | 月报 PDF、LLM 报告、泄露预案 |
| ③ AI 服务 | **统一隐空间世界模型**：Encoder + Latent Space + Predictor + 异常/通知/报告/追踪 Decoders。合成数据预训练 + 自监督学习 + 个人化 | 步态子空间 Encoder、干预推理、跨家庭泛化、生命体征异常 |
| ④ App | iOS 全功能 | Android、月报 PDF |

---

## 3. 核心架构：统一隐空间世界模型

家安 v3.0 的技术底座不再是 7 个独立算法模块组成的管线，而是一个**统一隐空间世界模型**。

### 3.1 架构范式

```
传统管道架构（v2.0，已废弃）:
  点云 → ALG-0(姿态标签) → ALG-1(拼接) → ALG-2(状态机) → ALG-6(通知)
                ↓                    ↓              ↓
          信息被硬决策丢弃      信息被硬决策丢弃   信息被硬决策丢弃

统一隐空间世界模型（v3.0）:
  点云 → Encoder → 隐空间 S_t（唯一真相源，无硬决策）
                      ↓
               Predictor: S_{t+1} = f(S_t, I_t)
                      ↓
          ┌──────────┼──────────┐
          ↓          ↓          ↓
      Decoder-A  Decoder-B  Decoder-C
      (跌倒)     (偏离)     (报告)
```

### 3.2 核心组件与 ALG 映射

| 组件 | 职责 | 包含的原 ALG | 分期 |
|------|------|-------------|------|
| **Encoder（编码器）** | 多模态输入 → 统一隐空间 S_t。点云编码（PointNet-like）+ 时间编码 + 多雷达 cross-attention 融合。不输出离散标签，输出隐空间向量 | ALG-0 点云→姿态（不再是独立模块，是 Encoder 的内部能力）+ ALG-1 多雷达融合（不再是规则拼接，是 cross-attention 学出来的） | MVP |
| **Latent Space（隐空间）** | 图部分（空间拓扑、区域类型、表面性质）+ 向量部分（人的位置/姿态/运动/生命体征编码、观测不确定性、全局上下文）。唯一真相源，所有下游任务从这里读取 | ALG-1 空间拓扑（变成隐空间的图结构）+ ALG-3 个人基线（变成隐状态流形上的分布估计） | MVP |
| **Predictor（预测器）** | S_t + 可选干预 I_t → S_{t+1}。自监督学习"人+屋子"的动力学。多尺度头：1s/10s/60s/3600s 预测。预测偏差本身就是异常信号 | ALG-2 跌倒延迟确认（不再是外部状态机，是 Predictor 预测"接下来应该发生什么" vs 实际发生了什么） | MVP |
| **Decoder: 异常检测** | 从 S_t 读取当前状态在该上下文中偏离"正常"的距离 → 异常分。空间感知：同样姿态在不同区域有不同的异常基准 | ALG-4 异常偏离 + ALG-5 多信号融合（不再独立运行，从隐空间直接读多维度偏离） | MVP |
| **Decoder: 通知决策** | 异常分 + 持续性 + 紧急性先验 → 通知等级 | ALG-6 分级通知 | MVP |
| **Decoder: 报告生成** | 过去 N 天的隐状态轨迹摘要 → 自然语言/统计数据 | 原周报统计 | MVP |
| **Decoder: 追踪** | 位置编码在图上随时间的变化 → 轨迹/热力图 | ALG-1 跨房间追踪 | MVP |
| **干预接口** | 干预向量 I_t 注入 Predictor。支持反事实推理："如果通知了子女 → 会怎样？""如果雷达离线 → 影响多大？" | 新增能力 | P2 |
| **Encoder: 步态子空间** | 从点云微多普勒提取步态特征 → 隐空间的步态子空间 | ALG-7 步态 | Phase 2 |

### 3.3 为什么统一隐空间

**不是工程偏好，是产品需求的必然：**

| 问题 | 管道架构 | 统一隐空间 |
|------|---------|-----------|
| 姿态标签在"床"vs"浴室地面"的语义不同 | 下游模块只有 `posture=lie, room="bathroom"` 字符串，不知道"浴室地面"和"床"的区别 | 隐空间里"位置编码"和"姿态编码"在同一个向量空间，直接计算语义距离 |
| 多个弱信号（活动量↓ + 睡眠↓ + 心率↑）叠加判断 | 各模块独立运行，输出各自分数再做加法——相关性丢失 | 所有信号在隐空间中自然耦合，偏离方向是否一致自动体现 |
| 雷达信号丢失时追踪断裂 | ALG-1 基于规则匹配，丢信号 = 丢追踪 | Predictor 用上一时刻状态 + 运动模型外推，填补信号空白 |
| 冷启动（新家、新老人） | 14 天基线窗口，期间没有模式分析 | 合成数据预训练 Encoder + Predictor → 部署即有基础世界常识（"人能走、不能穿墙、床上躺着正常"） |

### 3.4 训练策略

| 阶段 | 数据 | 目标 |
|------|------|------|
| **阶段 0：合成数据预训练** | 雷达射线追踪 + 人体骨骼模型 + 室内户型生成 | Encoder 学会"什么是人形点云"；Predictor 学会基础物理约束（人不能穿墙、运动是连续的）。部署即有基础能力 |
| **阶段 1：自监督学习** | 内测用户 24h 点云流 | 预测下一帧隐状态。所有正常日子的数据都是训练信号，无需标注。自然学到：空间连通性、典型行为模式、停留区域 |
| **阶段 2：个人化** | 单用户 14 天数据 | 在已有隐空间上建立个人基线的统计描述——"这个人的正常 = 隐状态在某个流形上的分布" |
| **阶段 3：跨家庭泛化** | 大规模用户数据 | 预训练世界模型底座 → 新用户 fine-tune 自家空间拓扑。数据飞轮闭环 |

### 3.5 冷启动与 fallback

- **部署即有**：合成数据预训练的 Encoder 提供基础感知（人形检测、姿态分类），不依赖真实用户数据
- **14 天学习期**：个人基线和模式偏离在第 15 天上线。期间由 Predictor 的"预测偏差"做基础异常检测（"预测器没想到会这样"本身就是信号）
- **Fallback 规则层**：系统内置轻量规则引擎作为安全网——当隐空间模型置信度低时（如新雷达型号、极端环境），退回到规则判断。这不是退回到管道架构，是世界模型训练期的安全带

---

## 4. 成功指标（MVP 验证）

| 指标 | 目标 | 测量方法 |
|------|------|---------|
| 跌倒检测准确率 | ≥ 90% | 模拟跌倒 + 日常活动对照 |
| 误报次数 | < 2 次 / 周 / 户 | 用户反馈 + 事件日志 |
| Predictor 预测误差 | 优于 naive baseline（静止假设）50%+ | 离线评估：预测点云 vs 真实点云 |
| 隐空间一致性 | 同一老人在同一区域同一姿态，不同时间的隐状态余弦相似度 > 0.85 | 离线评估 |
| 子女 App 日活 / 周活 | 日活 40%+ / 周活 70%+ | 数据打点 |
| NPS | ≥ 30 | 内测结束问卷 |

> 付费意愿等商业验证指标归 BP。
>
> 注：ALG-2 延迟确认有效率等管线时代指标，在世界模型架构中被 Predictor 预测偏差和 Decoder 异常分取代，不再独立测量。 |

> 付费意愿等商业验证指标归 BP。

---

## 5. 开放项汇总

高层开放项见下；各子系统与算法的细项分见对应文档（① OQ-E*、② OQ-B*、④ OQ-U*、算法文档 OQ-1~9）。

| 编号 | 事项 |
|------|------|
| OQ-1 | 手环 / 手表数据接入（纯雷达路线优先，后续再论证）。接入后作为 Encoder 的额外模态 |
| OQ-2 | 步态预测工程化（需 500+ 样本，P2）。作为隐空间的步态子空间 |
| OQ-3 | 老两口同时活动追踪。隐空间需扩展为多人状态向量，Predictor 需建模多人交互动力学 |
| OQ-4 | MVP 布点是否安排相邻 / 重叠户，以验证隐空间的空间拓扑自学习能力 |
| OQ-5 | 合成数据预训练的 Encoder 迁移到真实点云的 domain gap 有多大 |
| OQ-6 | 隐空间维度与图结构的超参：图节点粒度（房间级 vs 区域级）、向量维度（候选 256/512） |
| OQ-7 | 自监督学习目标函数选型：对比学习（SimCLR-like）vs 掩码预测（MAE-like）vs 直接预测下一帧隐状态（JEPA-like）

---

## 附录

### A · 硬件演进（供应链，非产品功能）

选型标准：不看"谁的固件准"，看"谁的点云质量好 + SDK 开放度高"。MVP 开发用 IWR6843ISK 开发板（USB 直插，mmWave SDK 直接输出点云）；量产用 TI IWR6843 模组 + 自研 WiFi/电源板；Phase 2 量 > 5,000 启动自研一体板（TI IWR6843 单 PCB）；Phase 3 量 > 5 万 + SDK 开放度达标转国产 60GHz SoC（首选加特兰 Alps，备选矽典微）。

### B · 术语

| 术语 | 含义 |
|------|------|
| 点云流 | 雷达每帧输出的 `[{x, y, z, velocity, intensity}]` 序列，云端 AI 的原始输入 |
| 个人基线 | 某老人连续 14 天数据统计得到的日常模式模型 |
| 数据聚合 | 多雷达事件/点云在云端统一时间轴拼接，非传感器融合 |
| 延迟确认 | 跌倒初判后等 3-5 秒观察后续行为再判决，降误报的核心机制 |
| 分级通知 | 不提 / 推送 / 强推+短信 / 电话 四级 |

### C · 免责声明

家安 housafe 不是医疗器械，不做医学诊断。健康数据与报告仅用于提示"建议就医 / 建议关注"，不构成诊断依据。
