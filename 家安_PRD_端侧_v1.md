# 家安 housafe · 端侧/嵌入式 PRD

> 版本 v2.0 | 2026-07-09 | 架构变更：自研点云感知
> v1.0→v2.0 主要变更：不再集成厂商跌倒检测固件；端侧输出从"结构化事件"变为"原始点云流 + 基础预处理"；硬件选型标准从"固件准确性"变为"点云质量 + SDK 开放度"。
> 子系统 PRD ① / ④ · 负责角色：嵌入式工程师
> 范围：雷达设备端固件与软件——从雷达芯片取原始数据、生成点云帧、基础去噪、WiFi 直传云端、配网、断网重传、离线上报、指示灯控制、OTA、设备鉴权。
> 边界外：所有 AI 感知（点云→姿态→跌倒→生命体征）在云端完成。端侧不做姿态判断。
> 上游依赖：TI IWR6843 雷达芯片（MVP）。下游对接：《家安_PRD_后端_v1.md》。

---

## 1. 模块边界

**端侧负责**：驱动雷达芯片、配置 chirp 参数、运行距离-多普勒 FFT 生成点云帧、基础去噪、封装点云流并打时间戳、WiFi 直连云上传、配网、断网缓存重传、离线/断电上报、指示灯控制、OTA、设备鉴权。

**端侧不负责**：姿态识别、跌倒判断、生命体征提取、多雷达融合、个人基线、偏离检测、通知决策——这些全部在云端 AI 服务（ALG-0 → ALG-6）。端侧只产出**单雷达的点云流**。

---

## 2. 功能需求

### 2.1 数据采集与处理

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-E1 | 雷达芯片驱动：配置 chirp 参数（带宽、斜率、帧率），运行距离-多普勒 FFT，生成每帧点云 `[{x, y, z, velocity, intensity}]` | MVP |
| FR-E2 | 基础点云去噪：CFAR 检测 + DBSCAN 聚类去鬼影。只做经典信号处理，不做姿态/跌倒判断 | MVP |
| FR-E3 | 点云封装：按帧打包，附时间戳（UTC 毫秒）、雷达 ID、房间标签 | MVP |
| FR-E4 | 安静态生命体征：在端侧判定安静状态（连续 N 秒点云速度方差 < 阈值），FFT 提取呼吸/心率频率。仅限经典信号处理，不依赖 ML | MVP |
| FR-E5 | 点云传输策略：每帧点云约 10-50 点 × 16 字节 ≈ 160-800 字节/帧，20fps → ~3-16 KB/s。正常 WiFi 网络实时传输 | MVP |
| FR-E6 | 点云本地只存环形缓存，不落盘。缓存满按策略丢弃，不上传即消除（合规） | MVP |

### 2.2 连接与上传

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-E7 | 数据上传：2.4G WiFi 直连云，每雷达独立设备身份，无本地网关。协议候选 MQTT over TLS（点云帧 body）或 WebSocket | MVP |
| FR-E8 | 断网处理：本地环形缓存，断网期间暂存点云帧，恢复后按序重传；缓存满丢弃旧帧并标记 | MVP |
| FR-E9 | 配网：扫描雷达底部二维码 → BLE/SoftAP 配 WiFi → 上线确认 | MVP |
| FR-E10 | 离线/断电检测：定时心跳，云端据心跳缺失判离线 | MVP |
| FR-E13 | 设备身份与鉴权：每雷达唯一 ID + 密钥/证书，直连云鉴权 | MVP |

### 2.3 设备管理

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-E11 | 指示灯控制：单 LED 白光，App 可远程开关 | MVP |
| FR-E12 | OTA：固件远程升级，失败回滚 | MVP |

### 2.4 Phase 2

| 编号 | 需求 | 分期 |
|------|------|------|
| FR-E14 | 端侧轻量推理：在雷达本地跑轻量姿态分类模型，用于网络不稳定时的本地 fallback 跌倒检测 | P2 |
| FR-E15 | 断电前最后消息：超级电容供电 2-3 分钟，断电时上报最后状态 | P2 |

---

## 3. 数据契约（端侧 → 云端）

```
点云帧   {ts, radar_id, room, frame_id, points: [{x, y, z, velocity, intensity}]}
生命体征 {ts, radar_id, room, quiet:true, resp_rate, heart_rate, quality}
心跳     {ts, radar_id, status, fw_version}
```

时间戳：端侧打，UTC 毫秒。云端据此做 ALG-1 时序对齐，要求端侧时钟漂移可被估计（秒级对齐精度即可）。

带宽估算：每帧 10-50 点 × 16 字节/点 × 20fps = 3-16 KB/s。一个雷达每天约 260MB-1.4GB。典型家庭宽带上行 5-10Mbps → 32-160KB/s 占用率较低。

## 4. 硬件选型与演进路线

### 4.1 选型标准（v2.0）

| 维度 | 标准 |
|------|------|
| 选型依据 | 点云质量 + SDK 开放度（能否拿到原始点云流） |
| 关键参数 | 点云密度、距离分辨率、角度分辨率、帧率、SNR |
| 数据输出 | 原始点云 (x,y,z,velocity,intensity) |
| 一票否决项 | SDK 不开放原始点云输出 |

### 4.2 演进路线

| 阶段 | 方案 | 成本 | 说明 |
|------|------|------|------|
| MVP 原型 | 海凌科 LD6002 60GHz 生命体征模组 | ~50 元 | 快速搭建系统链路。仅输出呼吸/心率数值，不输出点云——仅用于原型阶段打通数据管道，不用于模型训练 |
| MVP 开发 | IWR6843ISK 开发板 | ~$149 (~¥1,000) | 核心开发平台。3TX/4RX，AOP 封装，USB 直插，mmWave SDK 直接输出点云流 |
| MVP 产品 | TI IWR6843 模组 + 自研 WiFi/电源板 | ~200 元 | 量小先外购模组 |
| Phase 2 | 自研一体板（TI IWR6843 + WiFi + 电源，单 PCB） | ~100 元 | 量 > 5,000 |
| Phase 3 | 国产 60GHz SoC | ~60 元 | 量 > 5 万 + SDK 开放点云输出 |

### 4.3 Phase 3 国产候选

| 厂商 | 产品 | 优势 | 待验证 |
|------|------|------|--------|
| **加特兰**（首选） | Alps 系列 60GHz SoC | 国产出货量最大；集成 TinyML 芯片；支持级联做 4D 成像 | SDK 是否开放原始点云 |
| 矽典微 | XenD101HS 等 | 超低功耗（<1mW）；AiP 封装免射频设计 | 点云密度/质量是否满足跌倒检测 |

---

## 5. 非功能需求

| 编号 | 需求 | 目标 |
|------|------|------|
| NFR-E1 | 功耗 | < 2W |
| NFR-E2 | 点云上传时延（正常网络） | ≤ 200ms |
| NFR-E3 | 时间戳精度 | 时钟漂移可被云端估计校正，满足秒级窗口对齐 |
| NFR-E4 | 断网缓存 | ≥ 数小时点云数据，恢复后不丢序 |
| NFR-E5 | 配网/OTA 成功率 | 目标待定 |

---

## 6. 依赖与开放项

| 编号 | 事项 |
|------|------|
| OQ-E1 | 上传协议选型（MQTT / WebSocket）需与后端 PRD 对齐 |
| OQ-E2 | TI IWR6843 点云输出帧率与质量是否满足云端模型需求 |
| OQ-E3 | 点云上云隐私 PIA 评估（能否从点云重建室内活动） |
| OQ-E4 | 国产 SoC 的 SDK 开放度评估（Phase 3 前置条件） |
