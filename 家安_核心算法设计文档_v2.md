# 家安 · 核心算法设计文档

> 版本 v2.0 | 2026-07-09 | 架构变更：自研点云感知
> v1.0→v2.0 主要变更：移除"点云不上云"约束；ALG-2 从厂商固件 AI 二次判断改为自研点云→姿态→跌倒端到端；ALG-1 从"核心壁垒"降级为"数据聚合"；新增 ALG-0 点云人体状态理解。
> 约定：给出问题定义、输入输出、技术路径（候选+倾向）、可行性依据、评估指标、数据需求、风险。**超参/阈值标注为"候选·待实验确定"，不在无真实数据时编造。**

---

## 0. 全局架构约束

这些约束由产品架构决策锁定，直接决定每个算法的输入边界和能力上限。

| 约束 | 对算法的含义 |
|------|------------|
| **自研点云感知**：从雷达原始点云直接做人体状态理解 | AI 的输入是**原始点云流**，从源头避免了厂商固件的信息损失。可以在云端 GPU 上跑端到端时序点云深度模型，算法容量不受嵌入式 DSP 约束 |
| **点云上云**：雷达端侧不做重处理，原始点云通过 WiFi 直传云端做推理 | 云端 GPU 没有功耗约束，可以跑深度模型。代价是增加网络依赖和传输带宽（每帧 10-50 点 × 16 字节 × 20fps ≈ 10KB/s，WiFi 完全够） |
| **无本地网关**：每雷达 WiFi 直连云 | 多雷达数据在云端聚合，需处理跨设备时钟漂移与网络抖动 |
| **纯雷达**：不接手环/手表 | 无第三方加速度/PPG 数据交叉验证 |
| **无法识别身份**：雷达能数人数、测位置，不能回答"是谁" | 所有个人化算法建立在独居假设上。多人场景降级 |
| **学习期 14 天**：期间不推送模式类告警 | 依赖基线的算法冷启动不可用；不依赖基线的（跌倒）可即时工作 |
| **安静态生命体征**：呼吸/心率仅静坐/睡眠可靠 | 生命体征类算法须先判定安静状态再取值 |

**云端输入数据：**

```
点云流     {ts, radar_id, room, points: [{x, y, z, velocity, intensity}]}  ← 新增，取代厂商固件输出
存在事件   {ts, radar_id, room, presence:bool, moving:bool}                ← 自家 ALG-0 输出
姿态事件   {ts, radar_id, room, posture:[stand|sit|lie|walk|fall], confidence}  ← 自家 ALG-0 输出
生命体征   {ts, radar_id, room, quiet:bool, resp_rate, heart_rate, quality}    ← 自家 ALG-0 输出
人数估计   {ts, radar_id, room, count}                                        ← 自家 ALG-0 输出
```

所有结构化事件由 ALG-0 从点云中提取。不再有"厂商固件给的置信度"——所有置信度都是自家的。

---

## 1. 算法总览

| 编号 | 算法 | 管线层 | 分期 | MVP 冷启动可用 |
|------|------|-------|------|--------------|
| ALG-0 | 点云→人体状态理解（取代厂商固件） | L0 | MVP | 是 |
| ALG-1 | 多雷达时序对齐 + 空间拓扑自学习 + 跨房间追踪 | 聚合层 | MVP | 拓扑需 7 天 |
| ALG-2 | 跌倒延迟确认（降误报） | L1 | MVP | 是 |
| ALG-3 | 个人基线建模 | L2 | MVP | 第 15 天起 |
| ALG-4 | 异常偏离检测 | L3 | MVP | 依赖 ALG-3 |
| ALG-5 | 多信号置信度融合 | L3 | MVP | 依赖 ALG-3/4 |
| ALG-6 | 分级通知决策 | L4 | MVP | 是 |
| ALG-7 | 步态提取 + 跌倒风险预测 | 预测 | Phase 2 | 否 |

**依赖关系：** ALG-0 是新的底座——所有上层算法不再依赖厂商固件输出，而是自己的点云理解结果。ALG-1 负责多雷达数据聚合。ALG-3 是个人化底座。

---

## 2. ALG-0 · 点云→多维度特征提取

> 定位：从原始点云流中提取全维度信号。姿态分类只是其中一个产出——成品模块已足够好。ALG-0 的核心价值在产出模块不提供的维度：速度分布、点云形变、微多普勒谱、活动片段。这些是上层基线建模、趋势偏离、步态分析的输入原料。

### 问题定义

从单雷达的连续点云帧流中实时提取：
1. **基础状态**：人体存在/移动状态、人数估计
2. **姿态与动作**：姿态分类（站/坐/躺/走）、跌倒初判
3. **活动量化**：每帧速度分布→时段级活动量指标（日均速度、速度方差、快速移动占比）
4. **生命体征**：安静态呼吸频率、心率（点云管线的副产品，无需单独模组）
5. **点云形变特征**：PCA 形态、质心轨迹——跌倒确认区分真摔和快速坐下的关键信号
6. **微多普勒特征**：chirp 间相位变化时频谱——Phase 2 步态分析的输入
7. **活动片段切分**：时序聚类切出连续行为片段，供基线建模使用

所有产出从同一个点云流提取，时序精确对齐、置信度可比。

### 输入 / 输出

- 输入：单雷达点云流 `[{ts, points:[{x,y,z,v,i}]}]`，帧率 15-20fps
- 输出：存在事件、姿态事件（含跌倒初判）、生命体征采样、人数估计（数据契约见 §0）

### 技术路径

毫米波雷达点云的特殊性决定了不能直接搬视觉/LiDAR 的模型：

- **极其稀疏**：每帧 10-50 点（LiDAR 是数万点），信息密度低
- **噪声特殊**：多径反射鬼影、微多普勒模糊、RCS 闪烁
- **无颜色/纹理**：纯空间坐标 + 径向速度 + 信号强度

#### 子模块 0a：点云预处理与去噪

- DBSCAN 聚类去鬼影（多径假点通常在空间上离散或速度异常）
- 基于时序一致性的点过滤（真实人体点的运动是连续的）
- 输出：清洗后的点云帧

#### 子模块 0b：存在检测与人数估计

比较简单——有人/无人/人数是基于点云聚类数量和持续性的经典信号处理问题。
- 候选：持续帧中有稳定点云聚类 → 有人；聚类数 → 人数。传统方法足够（CFAR + 聚类 + 卡尔曼跟踪），不需要深度学习。
- 倾向：传统 DSP 方法，轻量、可解释、无需训练数据。

#### 子模块 0c：姿态分类（核心深度模块）

- **候选 A（帧级点云分类，MVP 起步）：** 单帧点云 → PointNet++/DGCNN 提取空间特征 → 姿态分类（站/坐/躺/走）。学术界已有验证（mmWave 人体姿态识别论文 2022-2024）。
- **候选 B（时序点云序列，演进目标）：** 多帧点云序列 → 3D sparse conv / Transformer over point tokens → 姿态序列。融合微多普勒谱作为辅助特征。时序模型可以捕获单帧分类丢失的运动模式——例如快速坐下和跌倒的空间轨迹差异。
- 倾向：MVP 从 A 起步快速验证，内测积累标注后过渡到 B。

#### 子模块 0d：跌倒初判

- 从 ALG-0 的时序姿态输出中做初判，而非从固件拿结果。
- "姿态从站立/行走 → 快速变为躺下" + 高度骤降 → 标记为跌倒候选事件。
- 初判偏召回（宁可多报），后续 ALG-2 做延迟确认降误报。

#### 子模块 0e：安静态生命体征提取

- 判定"安静状态"（姿态为坐/躺 + 连续 N 秒点云速度方差 < 阈值）
- 安静态下，胸腔微动产生相位调制 → FFT 提取呼吸频率 + 心跳频率
- 此部分用经典信号处理方法（非深度学习），已有 TI 参考设计和学术验证

### 可行性依据

- 毫米波点云人体姿态分类：2022-2024 年多篇顶会/期刊论文验证 PointNet 系列在 mmWave 点云上的有效性
- TI IWR6843 提供完整的 raw data → point cloud 工具链（mmWave SDK）
- 生命体征提取：TI 参考设计 + 西京学院 2026 研究已验证精度

### 评估指标

- 姿态分类准确率 ≥ 90%（vs 人工标注）
- 跌倒初判召回 ≥ 95%（宁误报不漏报，假正由 ALG-2 过滤）
- 安静态心率 MAE < 2 bpm、呼吸 MAE < 1 次/分
- 点云上传延迟 < 200ms（WiFi 局域网）

### 数据需求

**这是整个系统数据需求最大、最难的模块。** 需要：
1. 不同家庭环境的雷达点云采集（墙壁材料/家具/多径不同 → 点云分布漂移）
2. 多人（年轻人+老人）的姿态标注：站立/坐下/躺下/行走/弯腰/蹲下
3. **跌倒点云标注**：模拟跌倒（不同方向、不同起姿）+ 类跌倒动作（快速坐下、蹲下取物）← 这个是最大难点

数据采集策略：MVP M4-M6 内测期，在 5-20 户部署雷达，同时采集(1)日常活动日志(2)按脚本执行的模拟跌倒+易误报动作。用这两类数据训练初版模型。

### 风险与开放项

- **R1（最大风险）：标注数据不足导致模型精度不如固件。** 厂商固件经过数年工程验证，我们从零开始。缓释：开发阶段同时跑固件作为对照基准；如果 MVP 自研不达预期，先用固件过渡，自研并行改进
- **R2：跨环境泛化。** 每个家庭点云分布不同（墙壁材料、多径），实验室训练的模型在新家可能退化。需在 5+ 不同家庭环境采集数据做 fine-tune
- **R3：网络延迟。** 点云上云带来 50-200ms 额外延迟。对跌倒检测（延迟确认窗口 3-5 秒）可接受；对实时语音交互场景不可接受（暂不在范围内）
- **OQ1：** 点云上云的隐私合规——点云不含视觉信息，但需要做 PIA 评估能否逆向重建室内活动
- **OQ2：** 模型部署在云端 GPU vs 边缘轻量模型（长期可能需要在端侧跑轻量版本做本地 fallback）

---

## 3. ALG-1 · 多雷达时序对齐 + 空间拓扑自学习 + 跨房间追踪

> 定位：**数据聚合**——一个房间一个雷达，各覆盖独立空间。ALG-1 把各雷达的事件流在统一时间轴上拼接成全屋行为序列。

### 问题定义

多个雷达各自独立上传点云/事件流，时钟有漂移、网络有抖动。要在云端把它们对齐到统一时间轴，自动学出房间空间邻接关系，从而支持跨房间追踪（"同一个人从 A 走到了 B"）。

### 输入 / 输出

- 输入：各雷达的事件流（带各自本地时间戳 + 服务器接收时间戳）
- 输出：① 对齐后的全屋事件序列；② 空间拓扑图；③ 跨房间追踪链

### 技术路径

1. **时序对齐**：不追求 NTP 级硬件同步。用服务器接收时间戳 + 每设备相对漂移估计做软对齐。秒级窗口即可（跨房间移动是秒级过程）。
2. **空间拓扑自学习**：统计每对雷达的"A 离开 → B 进入"共现频率。高共现 → 判定相邻。7 天数据收敛。
3. **跨房间追踪**：独居假设下退化为简单问题——"离开 A + 进入相邻 B"直接配对。多人时降级。

> 倾向：软时钟对齐 + 秒级窗口共现匹配 + 独居单目标假设。

### 评估指标

- 跨房间追踪准确率 ≥ 85%（单人场景）
- 时钟对齐误差 < 2s

### 风险

- **R1：** 两人同时活动时追踪准确率降到 60-70%，MVP 接受降级
- **R2：** MVP 2 雷达布在浴室+卧室，两房间通常不相邻 → 跨房间追踪验证场景有限。内测时需至少一户相邻布点用于验证

---

## 4. ALG-2 · 跌倒延迟确认（降误报）

> 定位修正：不再是"AI 二次判断厂商固件结果"，而是"在自己 ALG-0 的跌倒初判基础上，利用 3-5 秒时序窗口做延迟确认"。核心逻辑：等一等，看后续状态。

### 问题定义

ALG-0 初判偏召回（宁误报不漏报），会产生误报（快速坐下、蹲下取物、弯腰）。利用 3-5 秒缓冲窗口观察跌落后行为——真跌倒后通常长时间静止或缓慢爬起；假跌倒（坐下/蹲下）后快速恢复站立/行走。综合判决降误报。

### 输入 / 输出

- 输入：ALG-0 跌倒初判事件 + 该雷达前后 N 秒姿态序列 + 点云运动特征
- 输出：`{is_fall:bool, confidence, evidence}`，延迟 3-5 秒判决

### 技术路径

- **候选 A（状态机，MVP 倾向）：** 跌倒 = ALG-0 初判 + 跌落后行为（N 秒内恢复站立/行走 → 假跌倒，持续静止/爬起缓慢 → 真跌倒）。可解释、零训练数据即可上线。
- **候选 B（时序分类器，演进）：** 以姿态序列 + 点云特征为输入训练二分类器。需标注数据。

> 倾向：MVP 用 A（状态机），内测积累标注后过渡到 A+B 混合。

### 评估指标

- 准确率 ≥ 90%、召回优先（真跌倒不漏）、误报 < 2 次/周/户
- ALG-2 应过滤 ALG-0 初判中 ≥ 50% 的假跌倒

### 数据需求

内测期模拟跌倒脚本 + 日常易误报动作（坐下/蹲下/弯腰/躺下）对照 + 人工标注。

### 风险

- **R1：** ALG-2 的召回率上限由 ALG-0 决定——如果 ALG-0 漏报，ALG-2 无法补救。ALG-0 的召回是系统跌倒检测的硬天花板
- **R2：** 躺姿起摔（床上跌落）ALG-0 检测弱，专项评估

---

## 5. ALG-3 · 个人基线建模

（与 v1.0 基本一致，小幅更新）

### 问题定义

为每位老人学出专属日常模式分布，作为后续偏离检测的参照。基线属于用户资产，不跨用户训练。

### 输入 / 输出

- 输入：连续 14 天全屋对齐事件流（ALG-1 输出）
- 输出：个人基线模型——按 `时段 × 指标` 的稳健分布（中位数、MAD、分位数）

### 技术路径

- **候选 A（分时段稳健统计，MVP 倾向）：** 把一天切成若干时段，对每个"时段 × 指标"用中位数 + MAD 建模。指标含：日间活动时长、各房间停留时长、起床/入睡时间、如厕频率、夜间离床次数、静息心率/呼吸区间
- **候选 B（时序聚类，演进）：** DTW/KMeans 提取典型日模式。数据需求高、可解释性差、冷启动难

> 倾向：MVP 用 A。14 天每时段约 14 样本，聚类不稳。

### 评估指标

- 基线稳定性：第 15-28 天数据落在基线预测区间的比例
- 重建误差：用基线预测次日各指标 vs 实际

### 风险

- **R1：** 14 天可能不够覆盖周中/周末差异 → 考虑区分工作日/周末
- **OQ：** 基线随季节/长期健康变化的在线更新策略

---

## 6. ALG-4/5/6 · 偏离检测 / 多信号融合 / 分级通知

（与 v1.0 一致，无架构变更）

技术路线不变：稳健 Z-score（ALG-4）、朴素贝叶斯对数似然累加（ALG-5）、可解释严重度评分 + 阈值分级（ALG-6）。详见 v1.0 对应章节。

---

## 7. ALG-7 · 步态提取 + 跌倒风险预测（Phase 2）

> v2.0 关键变化：点云已上云，步态特征可以直接从云端点云流中提取，不再与架构冲突（v1.0 的"点云不上云 vs 步态需要微多普勒"矛盾已消除）。

### 技术路径

从云端点云流中提取微多普勒步态特征（步速、步幅、对称性），输入 LightGBM/Transformer 进行跌倒风险分类。

### 可行性依据

Nature Sci Rep 2025、Frontiers in AI 2024 已验证技术路径。点云已在云端 → 数据通路已通。500+ 真人样本待工程化。

### 风险

- **R1：** 500+ 样本采集周期长，需专门数据采集计划
- **R2：** 步态分析需要老人持续在雷达覆盖范围内自然行走足够时长（不像跌倒是一次事件），日常采集策略待定

---

## 8. 跨算法公共问题

### 8.1 冷启动策略（前 14 天）

- **即时可用：** ALG-0（点云→姿态/跌倒/生命体征）、ALG-1 拓扑学习（7天）、ALG-2 跌倒延迟确认、ALG-6 对跌倒的硬规则通知
- **学习期后可用：** ALG-3/4/5 及依赖基线的模式类告警

### 8.2 数据采集计划

MVP 内测期（M4-M5，5-20 户）是核心标注来源：
1. **模拟跌倒脚本 + 日常易误报动作对照**（喂 ALG-0 姿态分类 + ALG-2 降误报）——最重要，量最大
2. 内测户真实户型图 + 轨迹标注（评 ALG-1）
3. 事件日志 + 用户回访标注真实异常时段（评 ALG-4/5）
4. App 通知反馈埋点（喂 ALG-6 演进）

### 8.3 演进主线

- MVP 阶段：ALG-0 用帧级点云分类（候选 A）+ 传统生命体征提取；ALG-2 用状态机；ALG-3/4/5 用稳健统计/贝叶斯累加
- 内测积累标注后：ALG-0 过渡到时序点云模型（候选 B）；ALG-2 引入学习分类器；ALG-5 过渡到 LightGBM

---

## 9. 开放项汇总

| 编号 | 事项 | 归属 |
|------|------|------|
| OQ-1 | 自研 ALG-0 精度能否超过固件（最大技术风险） | ALG-0 |
| OQ-2 | 点云上云隐私合规：点云能否逆向重建室内活动（PIA 评估） | ALG-0 / 合规 |
| OQ-3 | MVP 姿态分类：帧级点云 vs 时序点云，性能差异待实测 | ALG-0 |
| OQ-4 | 跨环境泛化：实验室模型→新家庭后精度退化程度 | ALG-0 |
| OQ-5 | 网络延迟对 ALG-0 实时性的影响（点云上传→云端推理→返回） | ALG-0 |
| OQ-6 | MVP 布点是否安排相邻/重叠户以验证 ALG-1 | ALG-1 |
| OQ-7 | 基线窗口 14 天是否够；在线更新策略 | ALG-3 |
| OQ-8 | 各指标偏离阈值标定方案 | ALG-4 |
| OQ-9 | 通知升级间隔、静默时段等策略参数 | ALG-6 |
