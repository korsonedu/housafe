# 家安 · 核心算法设计文档

> 版本 v3.2 | 2026-07-19 | 收口 AI 选型决策（Encoder 架构/隐空间维度/GPU/时序聚合/开放项状态）；补充自监督训练原理（JEPA+时间连续性）；锁定 PyTorch 技术栈
> v2.0→v3.0 主要变更：算法不再以"独立管线模块（ALG-0~6）"组织，改为以"世界模型组件"组织。原 ALG 的能力映射为世界模型的子模块——ALG-0/1 进入 Encoder，ALG-1 拓扑进入 Latent Space 的图结构，ALG-2 被 Predictor 的预测偏差取代，ALG-3/4/5/6 变为轻量 Decoders。核心变化：从"各模块独立设计输入输出"到"所有组件围绕统一隐空间设计"。
> v1.0→v2.0 主要变更：移除"点云不上云"约束；自研点云→姿态→跌倒端到端。
> 约定：给出问题定义、输入输出、技术路径（候选+倾向）、可行性依据、评估指标、数据需求、风险。**超参/阈值标注为"候选·待实验确定"。**

---

## 0. 全局架构

### 0.1 架构范式

```
┌────────────────────────────────────────────────────────────┐
│                 统一隐空间世界模型                           │
│                                                            │
│  多模态输入                                                 │
│  ┌──────────┐                                              │
│  │ 雷达点云  │──┐                                           │
│  │ (多雷达) │  │   ┌─────────┐   ┌──────────┐              │
│  ├──────────┤  ├──→│ Encoder │→→→│ 隐空间    │              │
│  │ 时间编码  │──┘   └─────────┘   │  S_t      │              │
│  ├──────────┤                     │           │              │
│  │ (未来模态)│                     │ 图+向量   │              │
│  └──────────┘                     └────┬──────┘              │
│                                       │                      │
│                          ┌────────────┼────────────┐        │
│                          ↓            ↓            ↓        │
│                      Predictor    Decoder-A   Decoder-B      │
│                      S_t→S_{t+1}  (异常)      (通知)         │
│                          │                       │           │
│                          预测偏差                 │           │
│                          (本身=异常信号)           │           │
└────────────────────────────────────────────────────────────┘
```

### 0.2 与原 ALG 的映射

| 原 ALG | 原职责 | 在世界模型中的位置 |
|--------|--------|------------------|
| ALG-0 点云→姿态/存在/生命体征 | 单雷达点云→结构化事件 | **Encoder 的内部能力**。不再输出离散标签，输出隐空间向量 |
| ALG-1 多雷达聚合+空间拓扑 | 时序对齐+拓扑图+跨房间追踪 | **Encoder 的 cross-attention 融合** + **隐空间的图结构**。学出来的，不是规则 |
| ALG-2 跌倒延迟确认 | 3-5s 窗口观察跌落后行为 | **Predictor 的预测偏差** + **异常 Decoder**。预测"该发生什么" vs 实际发生什么 |
| ALG-3 个人基线建模 | 14 天稳健统计 | **隐空间流形上的分布估计**。不再按"时段×指标"做独立统计 |
| ALG-4 异常偏离检测 | Z-score vs 基线 | **异常 Decoder**。当前 S_t 到该上下文中预期分布的距离 |
| ALG-5 多信号置信度融合 | 贝叶斯证据累加 | **不需要了**。在隐空间中，多维度偏离自动耦合 |
| ALG-6 分级通知决策 | 严重度评分→通知等级 | **通知 Decoder** |
| ALG-7 步态+跌倒风险预测 | 微多普勒→步态→ML | **Encoder 的步态子空间** + 专门的步态 Decoder（P2） |

### 0.3 全局架构约束

这些约束由产品架构决策锁定：

| 约束 | 对世界模型设计的含义 |
|------|-------------------|
| **统一隐空间** | Encoder 不输出离散标签。所有信息无损压缩为向量。下游任务从隐空间读取，不做独立的特征提取 |
| **自研点云感知** | Encoder 从雷达原始点云直接编码到隐空间。点云的稀疏性（10-50 点/帧）是特征不是 bug——约束迫使模型真正理解场景 |
| **点云上云** | Encoder 在云端 GPU 运行。点云帧仅用于实时编码，原始点云不持久化（合规） |
| **无本地网关** | 多雷达 cross-attention 在云端完成。需处理跨设备时间漂移与网络抖动——这些不确定性在 Encoder 输出中体现为 uncertainty |
| **纯雷达，无摄像头** | 信息量受限 → 世界模型必须从稀疏信号中学习填补空白。这是雷达+世界模型的绝配 |
| **无法识别身份** | 隐空间人状态向量不包含身份特征。多人场景降级（P2 前） |
| **14 天学习期** | 隐空间图结构 7 天收敛，个人基线 14 天。Predictor 从 Day 0 开始自监督学习 |
| **安静态生命体征** | Encoder 的安静态判定（不是独立模块）→ 生命体征解码器仅在安静态子空间读取 |

---

## 1. 组件设计

### 1.1 Encoder（编码器）

#### 问题定义

将多雷达原始点云流 + 时间编码压缩为统一的隐空间向量 S_t。关键约束：
- 不输出离散标签（姿态/事件）。所有信息保留在连续向量中
- 多雷达数据在隐空间中自然融合，非规则拼接
- 输出每个维度的 uncertainty，供 Predictor 判断"信模型还是信观测"

#### 输入 / 输出

- **输入**：
  - 多雷达点云流：`[{radar_id, ts, points: [{x, y, z, velocity, intensity}]}]`
  - 时间编码：时辰（正弦循环编码）、星期（7维 one-hot）、季节
  - 帧率：15-20fps
- **输出**：
  - S_t：隐状态向量（候选维度 256/512）
  - Σ_t：逐维度的 uncertainty（对角协方差，供 Predictor 做 Kalman-like 更新）

#### 技术路径

**子模块 1a：单雷达点云编码（PointNet-like）**

毫米波点云的特殊性：
- 极其稀疏：每帧 10-50 点。不能直接用 dense 3D conv（LiDAR 的做法）
- 无颜色/纹理：纯空间坐标 + 径向速度 + 信号强度
- 噪声特殊：多径鬼影、微多普勒模糊、RCS 闪烁

候选架构（已收口）：

| 选项 | 判断 | 理由 |
|------|------|------|
| **A：PointNet++** | ✅ **锁定** | 层级采样天然适配毫米波稀疏点云（10-50 点/帧）。学术界 mmWave PointNet 已验证（2022-2024）。参数量 ~200K，CPU 推理 < 30ms/帧。对稀疏场景的鲁棒性优于 dense 方案 |
| B：SparseConv3D | ❌ 驳回 | 为 LiDAR 密集点云（万点级）设计。毫米波场景需要帧累积才能填满有效体素，延迟高且信息稀释。参数量大 5-10×，推理需 GPU |
| C：Transformer over point tokens | ❌ 驳回（远期可重新评估） | 10-50 个点不足以让 self-attention 学到有意义的 token 关系。等点云密度提升（P2 多雷达融合后等效点数增加）再重新评估 |

锁定：PointNet++ 做单帧编码，时序聚合（见 1b）。

**子模块 1b：时序聚合（sliding window）**

**为什么单帧稀疏不是问题：** 毫米波点云有两个结构性优势：
1. 每个点自带**径向速度**（Doppler）。走路的四肢和静止躯干有不同速度分布（微多普勒特征），这是雷达独有的"运动指纹"——10 个带速度的点比 100 个纯空间点信息量大。
2. 人在动，雷达从不同角度连续扫描。32 帧 × 20 点 = 640 个空间-速度采样点，2 秒窗口内等效点数足以重建姿态。**时序上下文弥补单帧稀疏，连续运动提供自监督信号。**

**聚合架构：**
- MVP：**Temporal ConvNet（TCN）**。参数量小（~2M）、训练快、因果卷积天然适配流式推理。16-32 帧滑窗 → 每帧输出 S_t
- 演进：**Temporal Transformer**。在 TCN 验证完管线后替换，换取更强长程建模
- 关键约束：每帧输出一个 S_t（不是每窗口输出一个），保证实时性

**子模块 1c：多雷达 cross-attention 融合**

多个雷达的 S_t^i（第 i 个雷达的隐状态）通过 cross-attention 融合：
- 雷达间的空间关系是学出来的（attention weights → 隐式拓扑），不是人定义的规则
- 输出：融合后的全屋隐状态 S_t
- 取代原 ALG-1 的"软时钟对齐 + 共现规则"

**子模块 1d：时间编码注入**

- 时辰：sin/cos 循环编码（24h 周期）
- 星期：可学习的 7 维 embedding
- 注入方式：拼接到全局上下文向量，或 AdaGN（adaptive group normalization）

**子模块 1e：生命体征编码（安静态）**

- 经典信号处理（非深度学习）：判定安静态 → FFT 提取呼吸/心率
- 输出注入隐空间的生命体征子空间
- 不是独立模块——是 Encoder 内的一条旁路。安静态判定标准来自姿态子空间（坐/躺 + 低运动方差）

#### 可行性依据

- 毫米波点云 PointNet 分类：2022-2024 多篇顶会/期刊验证
- Cross-attention 多模态融合：Transformer 标准能力，跨雷达即跨模态
- TI IWR6843 提供完整 raw data → point cloud 工具链
- 合成数据预训练：雷达射线追踪 + 人体骨骼模型可生成大量训练数据

#### 评估指标

- 隐空间一致性：同一人同一姿态同一区域，不同时间的 S_t 余弦相似度 > 0.85
- 编码延迟：点云到达 → S_t 输出 < 100ms
- 下游任务指标（跌倒准确率 ≥ 90% 等）——端到端验证

#### 数据需求

1. **合成数据（阶段 0）**：雷达模拟器 + 人体骨骼模型 + 室内户型生成。用于 Encoder 基础预训练
2. **真实点云数据（阶段 1）**：内测用户 24h 点云流。用于 fine-tune 和自监督学习
3. **标注数据（阶段 1+）**：模拟跌倒 + 日常易误报动作对照。量远小于管道架构——因为自监督学习不需要标注

#### 风险与开放项

- **R1（最大风险）：合成数据→真实点云的 domain gap。** 真实浴室（蒸汽、瓷砖反射）的点云分布与模拟有差异。缓释：合成数据仅用于基础预训练（"什么是人形"），真实数据做自监督 fine-tune
- **R2：点云稀疏性导致隐空间坍缩。** 如果大部分帧是空的（雷达盲区），隐空间可能坍缩为"没人"这一个状态。缓释：loss 中加权非空帧；时间上下文窗口让模型记住"刚才有人"
- **R3：稀疏点云能否支撑准确率。** [已收口] 单帧 10-50 点确实不能——但这不是单帧分类问题。每点自带 Doppler 速度（微多普勒指纹），32 帧时序窗口等效 640 个空间-速度采样点。学术界 mmWave+PointNet+Temporal 姿态分类达 85-90%。对家安来说足够：跌倒检测有三层确认（Encoder 变化 → Predictor 偏差 → Decoder 持续性），单帧误判被后两层过滤。**稀疏是隐私换准确率的刻意取舍，不是缺陷。**
- **OQ1（隐空间维度）：** [已收口] 锁 256 维。10-50 点/帧的输入信息量不足以支撑 512 维——高于 256 会导致过拟合（隐空间中有效信号被噪声维度稀释）。对比学习在低维隐空间中也更稳定。
- **OQ2：** 多雷达 cross-attention 的计算量——4 雷达 real-time 是否可行

---

### 1.2 Latent Space（隐空间）

#### 问题定义

隐空间是系统的"唯一真相源"——所有组件从它读取、往它写入。需要设计隐空间的结构：存什么、怎么组织、怎么更新。

#### 结构设计

隐空间 = 图部分 + 向量部分：

**图部分（慢变，数天~数周更新一次）：**
- 节点：学出来的空间区域。不是人工标注"这是床"，而是在点云密度+运动模式基础上聚类出来的功能区域
  - 每个节点的属性向量：区域类型倾向（连续值，如"可躺卧程度""过渡通道程度""停留区域程度"）、平均占用时长、危险等级
- 边：空间关系
  - 邻接（可达、不可达）
  - 平均过渡时间（从节点 A 到节点 B 需要多少秒）
  - 过渡频率
- 图结构 7 天内从数据中自动学习收敛。学习方式：统计"离开区域 A → 出现在区域 B"的共现频率和过渡时间

**向量部分（每帧更新）：**
- 人状态向量（如果多人则多个向量）：位置编码（映射到图上的位置）、姿态编码、运动方向+速度、生命体征编码、活动意图
- 全局上下文向量：时间编码（时辰/星期/季节）、家中人数估计、近期事件摘要（过去 N 分钟的隐状态变化摘要）
- 观测不确定性向量：每个维度的 confidence

**个人基线（概率密度估计）：**
- 14 天隐状态轨迹 → 学出"这个老人的正常"在隐空间中的概率密度分布
- 不是独立模块——是隐空间流形上的 GMM / KDE / normalizing flow
- 基线是每个用户的独立数据资产

#### 技术路径

- **图结构学习**：变分图自编码器（VGAE）从行为序列中自动推断图节点和边。或更简单：空间聚类（点云热点 + 停留时长）→ 图节点；共现统计 → 图边
- **个人基线**：Gaussian Mixture Model 在隐空间流形上拟合 14 天轨迹，或 autoregressive density estimator

#### 风险

- **R1：图节点粒度选择。** 太粗（房间级）→ 分不清床和地板。太细（1m² 级）→ 过拟合、泛化差。候选默认：3-8 个节点/房间
- **R2：学出来的图节点可能不对应人类理解的"房间""家具"。** 可解释性挑战。缓释：图节点可以和用户户型图做可视化对齐，但不强求

---

### 1.3 Predictor（预测器）

#### 问题定义

学习"人+屋子"这个系统的动力学演化：给定前 N 帧隐状态序列 + 可选干预 I_t，预测下一时刻的隐状态 S_{t+1}。

这是世界模型的核心。Predictor 的价值不只是预测——**预测偏差本身就是最重要的异常信号。**

#### 输入 / 输出

- **输入**：前 N 帧隐状态序列 [S_{t-N}, ..., S_t] + 可选干预编码 I_t
- **输出**：预测的 Ŝ_{t+1} + 预测 uncertainty
- **训练目标**：minimize distance(Ŝ_{t+1}, real_S_{t+1})，其中 real_S_{t+1} = Encoder(真实点云_{t+1})

#### 技术路径

- **候选 A（Transformer over latent tokens，MVP 倾向）**：隐状态序列 → causal transformer → 下一帧预测。Llama/GPT 风格的 causal attention，但 token 是隐状态向量不是离散词
- **候选 B（RNN/LSTM/GRU，轻量）**：隐状态 → recurrent → 预测。计算量小但容量低
- **候选 C（Graph Neural ODE，远期）**：在图结构上建模连续时间动力学。更优雅但训练更难

倾向：MVP 用 A。Transformer 在时序预测上已验证，且和 LLM 的训练范式对齐（causal language modeling → causal state modeling）。

#### 多尺度预测

同一个 Predictor 带多个预测头：
- 1s 头：预测下一帧（20 frames ahead）——用于实时异常（跌倒）
- 10s 头：预测 200 帧后——用于短时行为预测
- 60s 头：预测 1200 帧后——用于行为模式
- 3600s 头：预测 72000 帧后——用于长期偏离

多尺度联合训练。短尺度更依赖当前运动惯性，长尺度更依赖行为先验（"这个时间通常做什么"）。

#### 物理约束学习

不需要显式编码物理规则。从数据中自然学到：
- 人不能穿墙（点云序列中从未观察到穿墙 → Predictor 不会预测穿墙）
- 运动轨迹连续（速度分布有物理上限）
- 某些区域之间不可直接到达（必须经过走廊等过渡区域）

#### 预测偏差作为异常信号

替代原 ALG-2 状态机的核心机制：

```
如果 Ŝ_{t+1}（预测） 和 real_S_{t+1}（真实编码） 的距离 > 阈值：
  → "系统没想到会这样" 
  → 异常 Decoder 进一步判断：是噪声还是真异常
  → 结合上下文（区域类型、时间、持续性）输出异常分
```

优势 vs 状态机：
- 不需要枚举"什么算正常、什么算异常"——学出来的
- 空间感知：在浴室预测躺下 = 意外；在卧室预测躺下 = 意料之中
- 可自适应：老人的日常模式改变后，Predictor 学到新模式，旧的异常变成新的正常

#### 干预注入（P2）

Predictor 接受干预向量 I_t：
- "通知子女" → 干预编码注入 → Predictor 预测干预后的轨迹
- 对比有干预 vs 无干预的预测差异 → 反事实推理

#### 评估指标

- 预测误差（MSE / cosine distance between Ŝ_{t+1} and real_S_{t+1}）
- 预测误差优于 naive baseline（"人不动"= S_{t+1}=S_t）50%+
- 多尺度预测的一致性：短尺度预测累加应该 ≈ 长尺度预测

#### 风险

- **R1：数据不平衡。** 大部分时间是"没人"或"人在床上不动"。Predictor 可能坍缩为"预测没人"这个 safe bet。缓释：loss 中加权非空帧/变化帧；curriculum learning——先学有人的片段
- **R2：长尺度预测质量。** 1 小时后的预测几乎不可靠。缓释：长尺度预测不做点预测，做分布预测（预测"正常范围"而非精确状态）
- **OQ：** 多尺度训练的损失权重——短尺度和长尺度的 trade-off

---

### 1.4 Decoders（解码器）

Decoder 是轻量的——不做特征提取，只做"从隐空间翻译为任务输出"。

#### 1.4.1 异常 Decoder

**问题定义**：从当前隐状态 S_t 判断"这是异常吗？异常程度？"

**输入**：S_t（当前隐状态）、P(S_t|context)（该上下文中的预期分布）、Predictor 的预测偏差
**输出**：异常分（0-1）+ 异常类型 + 可解释的偏离维度

**技术路径**：
- S_t 到该区域+该时间的预期分布的 Mahalanobis 距离
- 多维度偏离自动耦合——不需要外部融合算法
- 空间感知：同样姿态在不同区域的异常基准不同（从隐空间的图结构自动获得）

#### 1.4.2 通知 Decoder

**输入**：异常分、持续性（连续异常帧数）、紧急性先验（跌倒>静止>偏离）
**输出**：通知等级（不提/推送/短信/电话）

**技术路径**：轻量 MLP，输出 4-class softmax。训练数据来自用户反馈（通知后子女的确认/忽略行为）。

#### 1.4.3 报告 Decoder

**输入**：过去 N 天的隐状态轨迹（S_t 序列采样）
**输出**：活动摘要、睡眠统计、异常清单、趋势对比

#### 1.4.4 追踪 Decoder

**输入**：位置编码在图上随时间的变化
**输出**：轨迹线段、热力图、各区域停留时长

---

## 2. 训练策略

### 核心原则：时间连续性 = 免费标注

隐空间是连续向量空间（不是离散标签），人的运动在物理世界中天然连续。这构成了自监督学习的核心信号：

```
核心约束: S_t ≈ S_{t+1}，但 delta = S_{t+1} - S_t 的大小和方向
          携带运动信息（方向、速度、姿态变化趋势）

坐→站的过程: S 空间中从"坐区域"到"站区域"的连续轨迹
             → 不需要标注"这一帧是 60%坐+40%站"
             → Encoder 从连续性中自然学到过渡态的表征

训练信号来源（JEPA 范式，见 LeCun 2022）:
  ✓ 时间相邻性: S_t 和 S_{t+δ} 的相似度 ∝ 物理状态相似度
  ✓ 预测一致性: Predictor 预测的 Ŝ_{t+1} 应与实际 S_{t+1} 一致
  ✓ 物理约束: 人不能穿墙、速度有上限、轨迹连续
  ✗ 不需要: 任何人工标注
```

### 2.1 阶段 0：合成数据预训练

**目的**：部署时 Encoder 已有基础感知能力，Predictor 已有基础物理常识。

**合成数据生成**：
- 雷达射线追踪模拟器：基于物理光学（FMCW 雷达模型）生成 synthetic point cloud
- 人体骨骼模型：SMPL/SMPL-X 在不同姿态（站/坐/躺/走/跌倒）下的点云
- 室内户型生成：参数化户型 → 墙壁、家具的反射模型 → 多雷达部署位置

**预训练目标**：
- Encoder：时间对比学习（同一人的相邻帧编码相近，不同人/姿态的编码远离）+ 合成数据自带姿态标签做辅助分类头（权重 0.1，仅辅助信号不依赖）
- Predictor：下一帧隐状态预测（自监督）

**数据量**：合成数据无限。问题是 domain gap——缓释见风险。

### 2.2 阶段 1：自监督学习（真实数据）

**数据**：内测用户 24h 点云流

**训练目标**：
- 给定前 N 帧，预测下一帧隐状态（JEPA-like：Joint Embedding Predictive Architecture）
- 不需要任何标注。时间连续性 + 预测误差 = 自监督信号
- 核心损失：
  ```python
  # Encoder: 相邻帧编码相近
  L_contrastive = 1 - cos_sim(S_t, S_{t+1})
  # Predictor: 预测和实际一致
  L_predictive = smooth_l1(Ŝ_{t+1}, S_{t+1})
  # 总损失
  L = L_contrastive + L_predictive
  ```

**训练方式**：
- 每个家庭维护独立的世界模型实例（图结构不同）
- Encoder/Predictor 底座跨家庭共享
- 在线学习：持续从新数据中更新（类似 fine-tune），不需要离线重训

### 2.3 阶段 2：个人化

**数据**：单用户 14 天隐状态轨迹

**目标**：在该用户的隐空间中建立个人基线的概率密度估计

**方法**：GMM / KDE / autoregressive density estimator 在 14 天轨迹上拟合

### 2.4 阶段 3：跨家庭泛化（规模后）

**目标**：预训练一个世界模型底座 → 新用户快速 fine-tune（< 3 天）自家的空间拓扑

**方法**：元学习（MAML-like）或 domain adaptation（fine-tune 图结构部分，Encoder/Predictor 底座冻住）

---

## 3. 跨组件公共问题

### 3.1 冷启动策略

| 部署后 | 可用能力 | 来源 |
|--------|---------|------|
| Day 0（安装） | 基础人形感知、跌倒检测、静止检测 | 合成数据预训练的 Encoder + 规则 fallback |
| Day 1-7 | 空间拓扑逐渐收敛、跨房间追踪准确率逐日提升 | 隐空间图结构自学习 |
| Day 8-14 | 个人基线学习中，Predictor 已适应该家庭 | 自监督学习 + 基线密度估计 |
| Day 15+ | 全功能：模式偏离检测、个人化异常判断 | 基线收敛 |

### 3.2 Fallback 规则层

系统内置轻量规则引擎作为安全网：

| 场景 | 世界模型状态 | Fallback 行为 |
|------|------------|-------------|
| 新雷达型号（Encoder 未见过） | Encoder confidence 低 | 退回到经典信号处理（CFAR + 聚类 + 跟踪） |
| 极端环境（严重多径/蒸汽） | 隐空间 uncertainty 高 | 降级：仅做存在检测 + 静止检测，不做姿态分类 |
| Predictor 预测误差持续异常 | 可能模型退化 | 触发报警：建议检查设备部署 |
| 用户主动关闭 AI 功能 | 世界模型暂停 | 纯规则引擎运行 |

**这不是退回到管道架构。规则层是世界模型的安全网，不是替代品。**

### 3.3 数据采集计划

世界模型的数据需求与管道架构不同：

1. **合成数据（开发期）**：雷达模拟器 + 人体模型 + 户型生成。用于预训练
2. **真实日常数据（内测期）**：内测用户 24h 点云流。**不需标注**——自监督学习
3. **标注数据（仅用于评估，不用于训练）**：模拟跌倒 + 日常易误报动作。量远小于管道架构，仅用于验证指标
4. **用户反馈（产品期）**：App 通知的确认/忽略行为 → 通知 Decoder 的训练信号

### 3.4 演进主线

| 阶段 | Encoder | Predictor | Latent Space | Decoders |
|------|---------|-----------|-------------|----------|
| MVP 早期（M1-3） | 合成数据预训练 PointNet++ | Transformer 自监督（合成数据） | 图聚类 + GMM 基线 | 轻量 MLP |
| MVP 内测（M4-6） | 真实数据 fine-tune | 在线自监督学习 | 图收敛 + 个人基线 | 同上，通知 Decoder 用反馈 fine-tune |
| P2 早期 | 时序 Transformer 替代 PointNet++ | 多尺度联合训练 | VGAE 图学习 | 报告 Decoder 接 LLM |
| P2 后期 | 多模态 Encoder（门磁/温湿度） | 干预注入 + 反事实推理 | 多人状态向量 | 步态 Decoder |
| P3 | 跨家庭预训练底座 | 元学习快速适配 | 自动户型发现 | 全任务在线学习 |

---

## 4. 开放项汇总

| 编号 | 事项 | 归属 | 状态 |
|------|------|------|:---:|
| OQ-1 | 合成数据→真实点云的 domain gap | Encoder | 待硬件验证 |
| OQ-2 | 隐空间维度与图节点粒度的超参 | Latent Space | [已收口] 隐空间 256 维；图节点 3-8/房间 |
| OQ-3 | 自监督目标函数选型（对比 vs 掩码 vs JEPA） | Predictor | [已收口] JEPA（时间对比+预测一致性联合） |
| OQ-4 | 多尺度 Predictor 联合训练的损失权重 | Predictor | 待实验 |
| OQ-5 | 数据不平衡（大量"无人"帧）导致隐空间坍缩 | Predictor | 已给方案（加权+curriculum），待实验验证 |
| OQ-6 | 多雷达 cross-attention 实时性 | Encoder | MVP skip，接口留好，P2 再评 |
| OQ-7 | 基线窗口 14 天是否够；在线更新策略 | Latent Space | 待真实数据验证 |
| OQ-8 | 各 Decoder 的轻量架构选型 | Decoders | [已收口] MVP 全规则/统计，演进 MLP，P2 报告接 LLM |
| OQ-9 | Fallback 与世界模型的切换逻辑 | 全局 | 待集成测试定义切换阈值 |
| OQ-10 | 点云上云隐私合规（世界模型下点云不持久化）| 全局 / 合规 | [已收口] 点云不落盘，仅存 S_t 序列 |

---

## 5. 具体工作拆解

### 5.1 分类：算法（写代码） vs AI 模型（训练+部署）

| 组件 | 类别 | 需要 GPU 训练？ | 需要训练数据？ | 核心依赖 |
|------|------|:---:|:---:|------|
| 生命体征提取 | **纯算法** | 否 | 否 | scipy/numpy 信号处理 |
| 隐空间图结构学习 | **纯算法** | 否 | 否（统计聚类） | sklearn/networkx |
| 个人基线 GMM | **纯算法** | 否 | 隐状态序列（Encoder 产出后） | sklearn GaussianMixture |
| Fallback 规则引擎 | **纯算法** | 否 | 否 | 业务状态机 |
| **Encoder** | **AI 模型** | 是（必须） | 合成数据 + 真实数据 | PyTorch, GPU, 合成数据管线 |
| **Predictor** | **AI 模型** | 是（必须） | 隐状态序列（自监督，无需标注） | PyTorch, GPU |
| 异常 Decoder（MVP） | **纯算法** | 否 | 个人基线模型 | scipy Mahalanobis |
| 异常 Decoder（演进） | **轻量 AI** | 否 | 少量带标注异常样本 | 轻量 MLP |
| 通知 Decoder | **轻量 AI** | 否 | 用户反馈（App 确认/忽略） | MLP 4-class |
| 报告 Decoder | **纯算法 + LLM (P2)** | 否 | 否 | 统计聚合 + LLM API |

**关键结论：MVP 的 Encoder + Predictor 是唯一必须训练的两个 AI 模型。其余都是纯代码。** 在 Encoder 就绪之前，世界模型的所有下游组件可用 AI placeholder 现有的规则输出（点云高度→姿态）作为临时隐状态替代，先行开发验证。

---

### 5.2 Encoder（AI 模型 · 最大工作量）

#### 做什么

从稀疏毫米波点云帧（10-50 点/帧，15-20fps）输出 **256 维**隐状态向量 S_t（已收口：256 足够编码 10-50 点/帧的信息量，512 带来过拟合风险）。不输出离散标签。

#### 网络结构（具体定义）

```
输入: N_frames × N_points × 5 (x, y, z, velocity, intensity)
      每帧点数可变 (10-50)，padding 到固定 max_points=64

Stage 1 · 单帧点云编码 (PointNet++ ✅ 锁定)
  ├─ Set Abstraction 1: 64 采样中心, radius=0.5m, MLP [64,64,128]
  ├─ Set Abstraction 2: 32 采样中心, radius=1.0m, MLP [128,128,256]
  ├─ Set Abstraction 3: 16 采样中心, radius=2.0m, MLP [256,256,512]
  └─ Global Max Pooling → 512-d per-frame feature

Stage 2 · 时序聚合
  MVP: Temporal ConvNet (TCN)
    ├─ Causal dilated convolution, kernel_size=3, dilations=[1,2,4,8]
    ├─ 4 层, 每层 512-d hidden, ReLU
    └─ 优势: 参数量 ~2M, 训练快, 因果卷积天然适配流式推理
  演进: Temporal Transformer (TCN 验证完管线后替换)
    ├─ 4 layers, 8 heads, d_model=512, d_ff=2048
    └─ 优势: 更强长程依赖, 代价是参数量 ~10M + 推理延迟 ×2-3

Stage 3 · 多雷达融合 (Cross-Attention)
  MVP: skip（仿真/内测阶段仅单雷达，接口留好）
  P2: 多雷达 temporal features → Cross-Attention → 融合 S_t

Stage 4 · 输出头
  ├─ Linear(512, 256) → S_t: 256-d 隐状态向量
  └─ Linear(512, 256) → Σ_t: 256-d uncertainty (对角 log-variance)
```

#### 训练数据

**合成数据（阶段 0，开发期必须）**

| 数据项 | 生成方式 | 工作量估计 |
|--------|---------|-----------|
| 雷达射线追踪模拟器 | 基于 FMCW 物理模型，给定人体位置+姿态 → synthetic point cloud。开源方案：使用 TI IWR6843 的 SDK 中的点云生成逻辑 + RaySAR/MATLAB Radar Toolbox 参数校准。或用 Unity + RadarSim 插件 | 2-4 周 |
| 人体姿态库 | SMPL/SMPL-X 骨骼模型驱动。姿态集：站/坐/躺/走/跌倒（8 种跌倒类型）+ 日常过渡动作（坐下/起身/翻身/弯腰）。总数 ~50 种姿态 × 多种体型 × 多种位置 | 1-2 周（用现成 SMPL） |
| 室内户型生成 | 参数化户型模板（卧室/客厅/卫生间/厨房/走廊）。5-10 种户型 × 3-5 个雷达部署位置 × 每种户型生成 50-100 个行为序列 | 1-2 周 |
| 行为序列生成 | 基于 transition probability 的日常行为模拟器：醒来→起床→去卫生间→去厨房→... 每个行为持续合理的时长。生成完整的 24h × N 天点云序列 | 1-2 周 |

**真实数据（阶段 1，内测期）**

- 内测用户 24h 点云流。不持久化原始点云（合规），仅用于实时 Encoder 推理 + 在线 fine-tune
- **不需要标注**。自监督训练：同一人的不同时间步编码相近（contrastive），不同的编码远离

**标注数据（仅用于评估）**

- 模拟跌倒 + 日常易误报动作（蹲下捡东西、快速躺沙发、洗澡时弯腰）
- 量：每类 50-100 段视频/点云标注。远小于传统管道架构（因为模型是自监督学习的）

#### 训练管线

```
阶段 0: 合成数据预训练
  ├─ 自监督: SimCLR-style contrastive loss
  │   ├─ 同一人的相邻帧 → 编码相近 (positive pair)
  │   └─ 不同人/不同姿态 → 编码远离 (negative pair)
  ├─ 辅助监督: 如果合成数据自带姿态/动作标签 → 轻量分类头
  │   (不依赖标签，仅作为辅助信号)
  ├─ 优化器: AdamW, lr=1e-4, batch_size=128, epochs=100-200
  ├─ GPU: 单卡 A100 40GB, 训练时间 ~2-3 天
  └─ 输出: pretrained_encoder.pt

阶段 1: 真实数据 fine-tune (online)
  ├─ 从 pretrained_encoder.pt 开始
  ├─ 自监督: 同一家庭的日常序列 → contrastive
  ├─ 在线学习: 每晚对新数据做 1-2 epoch fine-tune
  ├─ GPU: 单卡 A100 可同时服务 20-30 户的在线学习
  └─ 输出: 每个家庭共享 Encoder 底座，但保留独立副本
```

#### 评估

| 指标 | 方法 | 目标 |
|------|------|------|
| 隐空间一致性 | 同一人同一姿态不同时间 → 余弦相似度 | > 0.85 |
| 姿态线性可分性 | 冻结 Encoder → 在隐空间上训练线性分类器（姿态） | > 85% accuracy |
| 编码延迟 | 点云到达 → S_t 输出 | < 100ms |
| 跌倒检测 recall | 端到端（含下游 Decoder） | ≥ 90% |

#### 部署形态

```
housafe/ai/
├── encoder/
│   ├── model.py          # PointNet++ + Temporal Transformer + Cross-Attention
│   ├── checkpoint.py     # 模型加载/保存/版本管理
│   ├── inference.py      # ONNX/TorchScript 推理 engine（CPU fallback for dev）
│   ├── train_pretrain.py # 合成数据预训练脚本
│   └── train_online.py   # 在线 fine-tune worker
├── data/
│   ├── synthetic/        # 合成数据生成管线（独立于 encoder）
│   │   ├── radar_sim.py  # FMCW 雷达射线追踪模拟器
│   │   ├── human_model.py# SMPL 驱动 + 姿态库
│   │   ├── room_gen.py   # 参数化户型生成
│   │   └── seq_gen.py    # 日常行为序列生成器
│   └── loader.py         # PyTorch Dataset/DataLoader
├── configs/
│   └── encoder_mvp.yaml  # 训练/推理超参
└── tests/
    └── test_encoder.py   # 维度/延迟/一致性检查
```

#### 依赖与前置条件

- 合成数据管线必须先建好（无数据无法训练）
- PyTorch + CUDA 环境
- 当前 AI placeholder (`ai/placeholder/main.py`) 的规则输出可作为"假 Encoder"让下游组件先开发
- Encoder 是**整个 AI 链路的瓶颈**——没有它，Predictor 和 Decoder 都只能用规则模拟

---

### 5.3 Predictor（AI 模型 · 第二大工作量）

#### 做什么

给定前 N 帧隐状态序列 [S_{t-N}, ..., S_t]，预测下一帧 Ŝ_{t+1}。**预测偏差 d(S_{t+1}, Ŝ_{t+1}) 本身就是异常信号。**

#### 网络结构

```
输入: [S_{t-32}, ..., S_t]  32 帧 × 512-d 隐状态序列
      可选干预向量 I_t (256-d, P2 实现)

Stage 1 · Causal Transformer
  ├─ Input Projection: Linear(512, 512)
  ├─ Learned Position Embedding: 32 个可学习位置向量
  ├─ Transformer Decoder (causal): 6 layers, 8 heads, d_model=512, d_ff=2048
  │   └─ Causal mask 确保不偷看未来
  └─ Output: 32 帧 × 512-d contextualized features

Stage 2 · 多尺度预测头
  ├─ 1s head:   Linear(512, 512) → Ŝ_{t+1}
  ├─ 10s head:  Linear(512×4, 512) → Ŝ_{t+10s}  (aggregate 4 intermediate frames)
  ├─ 60s head:  Linear(512×8, 512) → Ŝ_{t+60s}
  └─ 3600s head: Linear(512, 512) → Ŝ_{t+3600s} (global pooling over context)

每个预测头同时输出预测和 uncertainty:
  Ŝ_head, log_var_head = head(context)
```

#### 训练

```
数据: Encoder 输出的 S_t 序列（自监督，不需要标注）
     任何正常日子的隐状态轨迹都是训练信号

损失函数:
  ├─ 主损失: Smooth L1 loss between Ŝ_{t+k} and real S_{t+k}
  │   各尺度加权求和: L_total = L_1s + 0.5*L_10s + 0.3*L_60s + 0.1*L_3600s
  └─ 正则: KL divergence on uncertainty (prevent infinite uncertainty)

数据平衡:
  ├─ 大量帧是"无人"或"人不动"。Predictor 容易坍缩为预测"没人"。
  └─ 解法: loss 加权——变化帧（|S_t - S_{t-1}| > 阈值）权重 ×5

优化器: AdamW, lr=3e-4, batch_size=64 (sequence samples)
GPU: 单卡 A100, 每户 1 天数据训练 ~30min
在线更新: 每晚 1 epoch, 新数据 + 随机采样 20% 旧数据防遗忘
```

#### 异常信号计算

```python
# 实时推理时
pred_S, pred_uncertainty = predictor(history_S, scale="1s")
real_S = encoder(new_pointcloud)
prediction_error = cosine_distance(pred_S, real_S)

# 结合历史误差分布
z_score = (prediction_error - running_mean_error) / running_std_error

# z_score > 3 → "系统没想到" → 送异常 Decoder 进一步判断
```

#### 部署形态

```
housafe/ai/
├── predictor/
│   ├── model.py          # Causal Transformer + 多尺度头
│   ├── inference.py      # 实时推理 engine
│   ├── train_online.py   # 在线 fine-tune worker
│   └── anomaly_signal.py # 预测偏差→异常信号计算
├── configs/
│   └── predictor_mvp.yaml
└── tests/
    └── test_predictor.py
```

#### 依赖

- **Encoder 必须先就绪**（Predictor 消费 S_t 序列）
- 可用规则 Encoder（placeholder）先开发和验证 Predictor 的训练逻辑

---

### 5.4 Latent Space 图结构（纯算法）

#### 做什么

从隐状态轨迹中自动学习房间的功能区域拓扑，不需要人工标注。

#### 算法步骤

```
输入: 过去 7 天所有 S_t 的位置子空间 (128-d 切片)
      或 在 Encoder 就绪前用点云质心 (x_mean, y_mean, z_mean) 替代

Step 1 · 空间点聚类
  ├─ 方法: HDBSCAN 或 MeanShift (自动选簇数，不需要预设"有几个区域")
  ├─ 输入: 所有帧的 (x, y, z) 质心坐标（人在哪）
  ├─ 输出: K 个空间簇 → K 个图节点
  └─ 关键参数: min_cluster_size = 停留 ≥ 60s 的帧数（过滤路过）

Step 2 · 节点属性计算
  对每个节点:
  ├─ 平均高度: avg(点云 z_max in 该节点) → 区分"可躺卧"(低) vs "站立"(高)
  ├─ 停留比例: 该节点帧数 / 总帧数 → 区分"常驻区" vs "路过区"
  ├─ 时段分布: 各时辰的停留频率 → 区分"卧室(夜)" vs "客厅(昼)"
  ├─ 危险等级: max(点云高度变化) → 跌倒高风险区(浴室)敏感度更高
  └─ 属性向量: [avg_height, stay_ratio, 6×hourly_prob, risk_score]

Step 3 · 边构建（空间可达性）
  ├─ 共现统计: 统计"离开节点A后出现在节点B"的频率
  ├─ 过渡时间: avg(离开A的时间戳 - 出现在B的时间戳)
  ├─ 边权重: 过渡频率 / 总出边频率
  └─ 过滤: 权重 < 0.01 的边删除（不可达/罕见过渡）

Step 4 · 周期性更新
  ├─ 每日增量更新（新一天的轨迹加入统计）
  ├─ 7 天首次收敛 → 14 天稳定
  └─ 户型变化（搬家具）→ 图自动适应性调整
```

#### 代码实现

```python
# housafe/ai/latent_space/graph.py 核心接口

class SpatialGraph:
    nodes: dict[int, Node]          # node_id → Node
    edges: dict[tuple[int,int], Edge]  # (from,to) → Edge

    @classmethod
    def from_trajectories(
        cls,
        trajectories: list[np.ndarray],  # [N_frames × 3] (x,y,z) per day
        min_stay_seconds: float = 60.0,
    ) -> "SpatialGraph":
        """从 7 天轨迹中学习空间图"""
        ...

    def update(self, new_day: np.ndarray) -> None:
        """增量更新一天的轨迹"""
        ...

    def locate(self, position: np.ndarray) -> int:
        """给定 (x,y,z) → 当前在图上的 node_id"""
        ...

    def transition_probability(self, from_node: int, to_node: int) -> float:
        """从 A 到 B 的转移概率"""
        ...
```

#### 依赖

- 只需要点云质心坐标（不需要 Encoder 的完整 S_t）
- 可在 Encoder 之前独立开发和测试
- 输出供 Decoder 使用（空间感知异常判断："同一姿态在不同区域异常基准不同"）

---

### 5.5 个人基线 GMM（纯算法）

#### 做什么

对每个老人的 14 天隐状态轨迹拟合概率密度分布。给定当前 S_t，输出"在这个上下文中，这个状态有多正常"。

#### 算法步骤

```
输入: 14 天隐状态轨迹 S 序列（每天采样 ~1/min → ~1440 帧/天 → ~20K 帧总计）
      每帧 512-d。或 Encoder 就绪前用规则特征替代（见下方）

Step 1 · 降维（可选）
  ├─ PCA: 512 → 32 维（保留 > 90% 方差）
  └─ 目的: GMM 在高维下不稳定，降维到 32 维后拟合效果好

Step 2 · 分上下文 GMM
  ├─ 按时间上下文集: {工作日/周末} × {凌晨/上午/下午/晚上/深夜} = 10 个上下文
  ├─ 每个上下文独立一个 GMM (n_components=5-10, 自动选 via BIC)
  └─ 每个 GMM 拟合该上下文中 S_t 的分布

Step 3 · 异常分计算（在线）
  ├─ 给定当前 S_t + 当前上下文
  ├─ 取对应 GMM → 计算 Mahalanobis distance 到最近高斯分量
  ├─ 归一化: percentile(Mahalanobis) in 该上下文的历史分布
  └─ 输出: anomaly_score ∈ [0, 1]

Step 4 · 在线更新
  ├─ 每日新增数据 → 重新拟合 GMM（计算量小，~1min per 上下文）
  └─ 基线窗口滑动: 保留最近 28 天，前 14 天权重 ×0.5
```

#### 代码实现

```python
# housafe/ai/baseline/gmm_baseline.py

from sklearn.mixture import GaussianMixture
from sklearn.decomposition import PCA

class PersonalBaseline:
    contexts: dict[str, GaussianMixture]  # "weekday_morning" → GMM
    pca: PCA

    @classmethod
    def fit(
        cls,
        trajectories: list[tuple[int, np.ndarray]],  # [(weekday_hour, S_t)]
        n_days: int = 14,
    ) -> "PersonalBaseline":
        """训练 14 天 → PCA + 分上下文 GMM"""
        ...

    def score(self, S_t: np.ndarray, weekday: int, hour: int) -> float:
        """返回异常分 0-1。0=完全正常, 1=极度异常"""
        ...

    def update(self, new_frames: list[tuple[int, np.ndarray]]) -> None:
        """每日增量更新"""
        ...
```

#### 依赖

- 需要 Encoder 产出 S_t（或规则特征替代）
- Encoder 就绪前，可用以下规则特征组合作为临时隐状态：
  - `[posture_onehot(5), confidence, moving, presence, resp_rate, heart_rate, hour_sin, hour_cos, weekday_emb(7)]` → ~20 维
  - 这组特征足够验证 GMM 基线的完整算法逻辑
- sklearn GaussianMixture + PCA（纯 CPU，无 GPU）

---

### 5.6 生命体征提取（纯算法 · 信号处理）

#### 做什么

从毫米波雷达的相位信号中提取呼吸率和心率。这是经典信号处理问题，不需要 AI。

#### 算法步骤

```
输入: 雷达 IF 信号的相位序列（或微多普勒频谱）
      安静态判定: posture in [sit, lie] AND moving=False AND velocity方差 < 阈值

Step 1 · 相位提取
  ├─ 从点云中筛选"静态点"（velocity ≈ 0, intensity 高）
  ├─ 提取该点的相位随时间的变化: φ(t)
  └─ 相位变化 ∝ 胸腔位移（呼吸 ~1-12mm, 心率 ~0.1-0.5mm）

Step 2 · 带通滤波
  ├─ 呼吸通道: Butterworth bandpass 0.1-0.5 Hz (6-30 bpm)
  ├─ 心率通道: Butterworth bandpass 0.8-2.5 Hz (48-150 bpm)
  └─ 阶数: 4th order, zero-phase (filtfilt)

Step 3 · FFT 频谱分析
  ├─ 滑动窗口: 20s 窗口, 50% overlap
  ├─ 加窗: Hann window → FFT
  ├─ 呼吸率 = 呼吸频带内最高峰频率 × 60
  ├─ 心率   = 心率频带内最高峰频率 × 60
  └─ 质量   = 峰值 SNR (peak / noise floor mean)

Step 4 · 时序平滑
  ├─ 中值滤波: 5 窗口 median → 消除瞬时跳变
  └─ 卡尔曼滤波: 可选，对呼吸率做轻量平滑（状态: [rate, rate_change]）

Step 5 · 安静态判定（前置）
  ├─ 不是所有帧都适合提取生命体征
  ├─ 条件: pose in [sit, lie] + moving=False + 连续 5s 点云速度方差 < ε
  └─ 输出: quiet=True/False → Encoder 的安静态子空间标志
```

#### 代码实现

```python
# housafe/ai/vital_signs/extractor.py

from scipy.signal import butter, filtfilt, spectrogram
import numpy as np

def butter_bandpass(lowcut, highcut, fs, order=4):
    """设计带通滤波器"""
    nyq = 0.5 * fs
    low = lowcut / nyq
    high = highcut / nyq
    b, a = butter(order, [low, high], btype='band')
    return b, a

def extract_breath_rate(phase_signal: np.ndarray, fs: float = 20.0) -> tuple[float, float]:
    """
    从相位信号提取呼吸率
    Args:
        phase_signal: 20s @ 20Hz = 400 samples
        fs: 采样率
    Returns:
        (breath_rate_bpm, quality)
    """
    b, a = butter_bandpass(0.1, 0.5, fs)  # 6-30 bpm
    filtered = filtfilt(b, a, phase_signal)
    windowed = filtered * np.hanning(len(filtered))
    fft = np.abs(np.fft.rfft(windowed))
    freqs = np.fft.rfftfreq(len(windowed), 1/fs)
    peak_idx = np.argmax(fft[(freqs >= 0.1) & (freqs <= 0.5)])
    peak_freq = freqs[(freqs >= 0.1) & (freqs <= 0.5)][peak_idx]
    quality = fft[peak_idx] / np.mean(fft)
    return peak_freq * 60, float(quality)

def is_quiet(posture: str, moving: bool, velocity_var: float, duration_s: float) -> bool:
    """判断当前是否为安静态"""
    return (
        posture in ("sit", "lie")
        and not moving
        and velocity_var < 0.05  # 候选阈值
        and duration_s >= 5.0
    )
```

#### 现实约束

- **当前仿真数据没有相位信号。** simulator/feed.py 产出的 VitalsFrame 是直接 `random.uniform` 的假数据
- **生命体征提取是唯一必须等真实硬件才能验证的算法。**
- 当前阶段（仿真期）：生命体征由 simulator 直接提供数值，跳过提取步骤。提取算法代码先写好、单元测试用合成正弦波验证，等硬件到位后接入
- 评估：合成信号上 SNR > 20dB 时，呼吸率误差 < 1 bpm，心率误差 < 3 bpm

#### 部署形态

```
housafe/ai/vital_signs/
├── extractor.py        # 呼吸/心率提取
├── quiet_detector.py   # 安静态判定
├── filters.py          # 滤波器设计
└── tests/
    ├── test_extractor.py    # 合成正弦波测试
    └── test_quiet.py
```

---

### 5.7 Fallback 规则引擎（纯算法）

#### 做什么

当世界模型 confidence 低时（Encoder 输出 uncertainty 高、新雷达型号、极端环境），退回到确定性规则保证安全。

#### 规则清单

```python
# housafe/ai/fallback/rules.py

class FallbackEngine:
    """
    世界模型的安全网，不是替代品。
    每个规则都可以独立开启/关闭。
    """

    # ── 跌倒检测（替代 Predictor 异常信号）──────────────────
    def detect_fall(self, frames: list[dict]) -> dict | None:
        """
        规则: 高度骤降 > 0.8m 在 1s 内
             + 之后 5s 姿势 = lie
             + 之后 10s 无站立
        → 触发 fall alert (severity=critical)
        """
        ...

    # ── 静止超时（替代异常 Decoder 的空间感知）──────────────
    def detect_stillness(self, duration_s: float, room: str, posture: str) -> dict | None:
        """
        卫生间: lying + still > 5min  → warning
                 lying + still > 10min → critical
        卧室:   lying + still → 不告警（正常睡觉）
        其他:   still > 30min → info
        """
        ...

    # ── 设备离线（独立于模型）──────────────────────────────
    def detect_offline(self, last_heartbeat_s: float) -> dict | None:
        """
        心跳超时 > 60s → warning
        心跳超时 > 300s → critical
        """
        ...

    # ── 生命体征阈值（独立于基线）──────────────────────────
    def check_vital_thresholds(self, hr: float, rr: float) -> dict | None:
        """
        心率: < 40 or > 130 → warning; < 30 or > 150 → critical
        呼吸: < 6 or > 30 → warning; < 4 or > 35 → critical
        """
        ...

    # ── 切换逻辑────────────────────────────────────────────
    def should_fallback(self, encoder_uncertainty: float, frame_quality: float) -> bool:
        """
        encoder_uncertainty > 0.7 OR frame_quality < 0.3
        → 切换到 fallback 引擎
        """
        ...
```

#### 部署

- 规则引擎常驻运行，与世界模型并行
- 决策级融合：世界模型正常时，规则引擎的输出作为"第二意见"（冗余安全）；世界模型降级时，规则引擎接管
- 每个规则可独立配置开关和阈值
- 当前阶段（无 Encoder）：规则引擎是 **唯一真实运行的异常检测**，不是 fallback

---

### 5.8 Decoders（轻量 · 算法为主）

#### 5.8.1 异常 Decoder（MVP 纯算法）

```
输入: S_t (512-d 或规则特征 20-d), 当前上下文 (hour, weekday, node_id)
输出: anomaly_score ∈ [0,1] + anomaly_type + 偏离维度

MVP 版本（无 AI 训练）:
  ├─ 个人信息: 调用 PersonalBaseline.score(S_t, weekday, hour)  → Mahalanobis 距离
  ├─ 空间感知: 查询 SpatialGraph → 当前区域的危险等级 + 典型姿态
  │   例: 浴室+lie → 危险区域×卧姿 → 加权
  ├─ 持续性: 连续 N 帧高异常 → 确认信号（单帧抖动忽略）
  └─ 输出: anomaly_score = Mahalanobis_percentile × 空间危险系数 × 持续性系数

演进版本（轻量 MLP, Encoder 就绪后）:
  ├─ MLP: 512 → 256 → 64 → 1 (sigmoid)
  └─ 训练数据: 规则引擎累积的异常标注 + 人工审核
```

#### 5.8.2 通知 Decoder

```
输入: anomaly_score, severity, duration_s, alert_type
输出: notification_level ∈ {none, push, sms, call}

MVP 版本（纯规则）:
  ├─ fall + critical → call
  ├─ fall + warning → sms
  ├─ stillness + critical → sms
  ├─ vital_anomaly + critical → sms
  ├─ pattern_deviation + warning → push
  └─ 其余 → push 或 none

演进版本（MLP 4-class, 有用户反馈后）:
  ├─ 训练数据: App 端子女对通知的确认/忽略/标记"误报"行为
  ├─ MLP: [anomaly_score, severity_onehot, duration, alert_type_onehot, hour, weekday] → 4-class softmax
  └─ 每个家庭独立训练（通知偏好因人而异）
```

#### 5.8.3 报告 Decoder

```
输入: 过去 N 天隐状态轨迹（采样后）
输出: 结构化统计数据

纯算法:
  ├─ 活动摘要: 各姿态占比、总步数估计、外出/在家时间
  ├─ 睡眠统计: 睡眠时长、入睡/醒来时间、碎片化次数
  ├─ 异常清单: 该周期内触发的所有异常（类型/时间/严重度/持续时长）
  └─ 趋势对比: 本周 vs 上周各指标 ±Δ

P2 接 LLM（API 调用，不训练/不部署）:
  ├─ 结构化数据 + prompt template → 自然语言报告
  ├─ 候选: DeepSeek / 通义千问（中文好+便宜）或 Claude Haiku / GPT-4o-mini
  └─ "张阿姨本周睡眠时间较上周减少 1.2 小时，周二凌晨有 1 次异常起夜..."
```

#### 5.8.4 追踪 Decoder

```
输入: 位置编码在图上随时间的变化（x,y,z 序列 + node_id 序列）
输出: 轨迹线段、热力图、各区域停留时长

纯算法:
  ├─ 轨迹平滑: 卡尔曼滤波（恒速模型）
  ├─ 热力图: 2D 高斯核密度估计
  └─ 区域统计: groupby node_id → 总停留时长/次数/平均时段
```

---

### 5.9 数据管线（纯工程 · 关键基础设施）

#### 5.9.1 合成数据生成管线（开发期必须 · 最大工程工作）

```
housafe/ai/data/synthetic/
├── radar_sim.py       # FMCW 雷达射线追踪模拟器
├── human_model.py     # SMPL 驱动 + 姿态定义
├── room_gen.py        # 参数化户型生成
├── seq_gen.py         # 日常行为序列生成器
├── render.py          # 将场景渲染为点云帧序列
└── export.py          # → PyTorch Dataset / TFRecord

工作流程:
  1. room_gen → 户型 (墙壁坐标、家具位置、区域标签)
  2. seq_gen → 24h 行为序列 (时间戳, 姿态, 位置, 动作)
  3. human_model → SMPL 骨骼在该姿态/位置的 mesh
  4. radar_sim → 对 mesh 做 FMCW 电磁仿真 → sparse point cloud
  5. export → (pointcloud_frames, metadata) 写入训练格式

输出量: ~100 种户型 × ~10 个 24h 行为序列 = ~1000 天数据
        每天 ~1.5M 帧 (@16fps) × ~20 点/帧 → ~30M 点云/天 → ~30B 总点云
        总存储 ~500GB (压缩后)
```

#### 5.9.2 真实数据流（内测期）

```
设备 → WebSocket ingest (已有) → Redis Stream (已有)
  → AI worker 消费 (已有 placeholder)
    → 帧累积 buffer (新): 累积到 32 帧 → 送入 Encoder
    → S_t 序列持久化 (新): 隐状态序列存入 Postgres/Timescale (仅 S_t, 不存点云)
    → 原始点云丢弃 (合规: 点云不落盘)
```

---

### 5.10 训练基础设施

**模型规模估算：**
| 模型 | 参数量 | 训练数据量 | 单卡训练时间 |
|------|--------|-----------|:---:|
| Encoder (PointNet++ + TCN) | ~10M (PN++ ~200K + TCN ~2M + 融合层) | 合成点云 ~500GB | 4090: 4-5 天 |
| Predictor (Causal Transformer) | ~20M (6层 d=512) | 隐状态序列（向量，很小） | 4090: 1 天 |

**结论：两模型参数量均在千万级，不属于大模型范畴。不需要 A100 集群。**

| 需求 | 方案 | 理由 | 分期 |
|------|------|------|------|
| GPU 训练 | **RTX 4090 24GB**（云 GPU 按需租用，如 AutoDL/恒源云 ~2元/时） | 模型小（< 20M params），4090 24GB 显存绑绑有余。训一轮 Encoder ~200 元。A100 的成本是 4090 的 10×，参数量不匹配 | MVP |
| GPU 推理 | MVP 期 **CPU 推理**（ONNX Runtime, batch=1） | 10-50 点/帧的 PointNet++ 在 CPU 上 < 30ms/帧。不需要 GPU | MVP |
| 实验追踪 | **WandB**（个人免费额度） | 比 MLflow 省运维，个人项目够用 | MVP |
| 模型版本管理 | **本地 checkpoint + WandB Artifacts**（或 git-lfs） | 量级不需要 S3。每个家庭独立 checkpoint 但底座共享 | MVP |
| CI 评估 | 每次 PR → 在 eval set 上跑下游任务指标（不跑训练） | MVP |
| 在线学习调度 | 夜间 2-4am 低负载时段 → cron 触发每户 online fine-tune | P1 |

---

### 5.11 构建顺序

```
Phase A · 算法基础（无 Encoder, 用规则特征）── 当前阶段可立即开始
  ├─ A1. 规则引擎 (fallback/rules.py)            ← 3-5 天
  ├─ A2. 图结构学习 (latent_space/graph.py)       ← 3-5 天
  ├─ A3. 个人基线 GMM (baseline/gmm_baseline.py)  ← 2-3 天
  ├─ A4. 异常 Decoder (decoders/anomaly.py)       ← 2-3 天
  └─ A5. 通知 Decoder (decoders/notification.py)  ← 1-2 天

Phase B · 合成数据 + Encoder ── AI 核心
  ├─ B1. 合成数据管线 (data/synthetic/*)          ← 4-6 周
  ├─ B2. Encoder 模型实现 + 训练                   ← 3-4 周
  └─ B3. Encoder 推理部署 + 接入现有 ingest 管线    ← 1 周

Phase C · Predictor
  ├─ C1. Predictor 模型实现                        ← 2-3 周
  ├─ C2. Predictor 训练 + 在线学习                  ← 1-2 周
  └─ C3. 预测偏差→异常信号接入异常 Decoder           ← 1 周

Phase D · 整体联调 + 演进
  ├─ D1. Encoder + Predictor + Decoders 端到端集成  ← 2 周
  ├─ D2. 通知 Decoder MLP (用户反馈训练)            ← P1
  └─ D3. 报告 Decoder + LLM 自然语言                ← P2
```

### 5.12 当前代码落点

```
housafe/ai/
├── placeholder/               # 已有，保留作为调试/对比基准
│   ├── __init__.py
│   └── main.py               # 规则模拟（点云高度→姿态）
├── vital_signs/               # Phase A · 新建
│   ├── __init__.py
│   ├── extractor.py
│   ├── quiet_detector.py
│   └── tests/
├── fallback/                  # Phase A · 新建
│   ├── __init__.py
│   ├── rules.py
│   └── tests/
├── latent_space/              # Phase A · 新建
│   ├── __init__.py
│   ├── graph.py
│   └── tests/
├── baseline/                  # Phase A · 新建
│   ├── __init__.py
│   ├── gmm_baseline.py
│   └── tests/
├── decoders/                  # Phase A · 新建
│   ├── __init__.py
│   ├── anomaly.py
│   ├── notification.py
│   ├── report.py
│   ├── tracking.py
│   └── tests/
├── encoder/                   # Phase B · 新建
├── predictor/                 # Phase C · 新建
├── data/synthetic/            # Phase B · 新建
├── configs/                   # Phase A · 新建
└── shared/
    ├── __init__.py
    ├── types.py               # 共享数据结构
    └── redis_client.py        # Redis Stream 消费封装
```

这就回答了"AI 在哪"：**Encoder + Predictor 是 AI（需要训练），其余全是算法（写代码）。Phase A 全部可以在没有硬件/没有训练数据的情况下直接写成产品级代码，用规则特征作为临时隐状态验证完整链路。**
