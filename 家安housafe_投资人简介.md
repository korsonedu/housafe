# 家安 housafe · 投资人简介

## 一、生意

家安 housafe 面向与父母异地居住的子女，销售全屋毫米波雷达硬件套装，用统一隐空间世界模型（ELPD）持续理解老人在家的状态与变化，AI 识别异常并按严重程度分级通知子女——让异地子女 30 秒看懂父母今天过得好不好，异常时第一时间知道。

## 二、核心竞争力

家安用**统一隐空间世界模型**取代传统算法管线，硬件外购。

传统养老监测产品是"管道架构"：传感器 → 规则 → 事件 → 通知。信息在每个环节被硬决策丢弃——姿态分类丢了原始点云，事件拼接丢了空间上下文，阈值判断丢了不确定性。

家安是"世界模型架构"：雷达点云 → Encoder 压缩为统一隐空间表征 → Predictor 学习老人的行为动力学 → 所有任务（跌倒/静止/偏离/报告）从同一隐空间通过轻量 Decoder 读取。核心差异：**系统不只是检测事件，它理解"屋子现在是什么状态，接下来应该发生什么"。**

| | 传统管道（竞品） | 家安世界模型 |
|---|---|---|
| 跌倒判断 | "姿态=fall + 房间=浴室" → 硬规则 | 隐空间中位置和姿态在同一个向量空间——"躺浴室地上"和"躺床上"自然区分 |
| 多信号融合 | 各模块独立打分后外部加法 | 所有信号在隐空间中自然耦合，偏离方向自动一致 |
| 雷达信号丢失 | 追踪断裂，重新匹配 | Predictor 用上一时刻状态外推，不丢追踪 |
| 冷启动 | 等 14 天数据积累 | 合成数据预训练 → 部署即有基础世界常识 |
| 模型进化 | 固件出厂定型，标注数据驱动更新 | 自监督学习：每一户的日常数据都是训练信号，无需人工标注 |
| 新任务扩展 | 新模块 + 新接口 + 新标注 | 新 Decoder 从隐空间读取，零额外标注 |

**三层壁垒：**

1. **范式壁垒（最高的）**——统一隐空间世界模型架构。这不是"做得更好"，是架构代际差异。就像 OpenAI 用 GPT 替代 N 个 NLP 模型，家安用一个统一的世界模型替代 N 个专用检测算法。竞品从管道迁移到世界模型，不是在改代码，是在改公司。

2. **算法壁垒**——毫米波雷达点云极其稀疏（每帧 10-50 点）、噪声特殊。需要同时懂雷达信号处理 + 点云深度学习 + 自监督表示学习的团队。竞品要么用厂商固件（硬件厂），要么用视觉模型（AI 厂但回避了雷达）。

3. **数据飞轮（时间壁垒）**——自监督学习不需要标注。用户越多 → 模型越强 → 产品越好 → 用户越多。14 天建立个人基线，3 个月比 14 天准，1 年比 3 个月准。切换成本随使用时间指数增长。

## 三、愿景

中国正在进入深度老龄化，1.2 亿独居老人。他们的健康状况，对远方的子女、对医疗和养老体系，长期是一个盲区。

家安用世界模型技术把这个盲区补上——让每一位在家变老的人都被持续看见、被及时守护。
